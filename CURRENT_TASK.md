# CURRENT TASK

更新时间：2026-07-17（Asia/Shanghai）

## 2026-07-17 当前权威进度：P000006交接终态、只读封存与P000007验收

- 已提交项目检查点：`98411ca feat: checkpoint AI Factory production workbench`。后续修复在该检查点之上继续，不改写历史商品资料。
- SQLite已升级到schema `003`。只要Ozon写入返回有效`task_id`，本地立即进入`HANDED_OFF_TO_OZON / 已提交Ozon / 100%`；不再显示99%、不等待商品ID、不安排远端回查，也不允许第二次CREATE/UPDATE。
- P000006三家店铺的历史task记录已从旧`PENDING_REMOTE`迁移到`HANDED_OFF_TO_OZON`。工作台现在显示“已提交Ozon，后续请在Ozon商品卡后台处理”，不会把旧`status.json`中的失败/95%覆盖到当前真实交接状态。
- 已提交商品在工作台彻底只读：标题、简介、标签、属性、SKU价格、店铺价格、店铺选择、AI建议、图片确认/重做/替换/删除、类目修改和再次运行均不可改；查看图片、复制URL和查看历史记录仍可用。后端接口也以409拒绝绕过页面的本地修改。
- 旧目录契约商品分成两类：未提交的旧商品硬阻断，必须重新通过当前工作台采集；已有task_id的历史商品只读展示，不要求伪造新版N+8确认清单。
- 关键安全测试已恢复到默认测试集：类目近义兼容、只读接口白名单、30个俄文标签、SKU主图缺失策略、上传payload无库存字段、库存端点拒绝、task_id防二次写、已有offer走UPDATE、内容未变走SKIP。完整回归475项，结果`OK (skipped=112)`，0失败、0错误、0资源泄漏警告；此前基线为462项、跳过126项。
- 当前正式共享工作台只有P000006，没有P000007或其他全新正式采集商品。因此P000007“不调用Ozon”的真实浏览器端到端验收尚未启动，不能用旧商品、测试附件或伪造目录代替。
- 本轮Ozon CREATE/UPDATE 0，Ozon只读/状态回查0，库存接口0；没有启动商品、图片或上传worker。

### 下一步唯一需要的真实输入

在1688页面使用当前插件新采集一件商品，手动选择SKU、最终Ozon类目和目标店铺。新商品进入共享工作台后，继续执行P000007本地全链路验收，只运行到`WAITING_MANUAL_REVIEW`，不调用Ozon。

## 当前权威任务：P0生产边界、批次快照和人工确认不可篡改

- 正式生产的唯一输入是 AI Factory 工作台当前采集形成的 `products/<product_id>/input`。每件商品必须同时匹配自己的 `product_id + collection_id`，并带有 `source_kind=workbench_collection`、`source_path`、`sha256` 和 `collected_at` 来源清单；缺失、跨商品、已归档或哈希变化都会在创建批次和运行商品前硬阻断。
- Codex 对话中手动提供的图片、规格、截图和文字只能作为开发测试素材，统一放在 `test-data/manual-input/<test_case_id>`；测试输出只进入 `test-data/manual-output/<test_case_id>`。`manual_test` 不得进入正式采集箱、商品主档、payload 或 Ozon 队列。
- 正式商品图片按物理目录分层：真实采集原图只在 `input/{sku-images,main-images,detail-images}`；AI候选只在 `output/generated-images/{variant-main,detail}`；拒绝图在 `output/rejected-generation`；人工确认图在 `output/accepted-images`。任何 `output` 路径进入商品参考图都会失败，生成图不会覆盖或回流 `input`。
- 通用图片契约是：已选 SKU 数量为 N（1-10）时，生成 N 张各自绑定真实 SKU 原图的独立主图，并生成正好 8 张商品组共享详情图，总数 N+8。手动模式只有人工明确确认的图片才能进入 `accepted-images`；确认前状态为“等待人工检查”，不自动打开预览、不上传。
- `ozon-ecommerce-designer` 已成为商品分析后的统一策划层。联网 Codex 不可用时任务暂停等待恢复，禁止使用低质量本地分析降级；图片阶段只执行已经完整落盘的设计方案和逐图提示词，不重新分析商品。
- 2026-07-12 的总提示词和 8 张参考截图已找到，并只复制到 `references/manual-ozon-flow-2026-07-12/` 作为 `layout_quality_baseline`，不作为任何正式商品的产品图。无效的 P900001 已完整迁出 `products/` 到 `test-data/offline-audit/P900001`；P900002 是手工测试身份，也只存在于 `test-data`。
- 正式商品编号只允许 `P000001-P899999`；`P900000-P999999`永久保留给测试/审计，不能出现在工作台、正式批次、payload或Ozon队列。当前下一正式编号会接真实生产序号，不会被P9样品抬高。
- 创建批次会在所有校验通过后一次性冻结每个商品的 `review_mode/auto_upload`、`collection_id` 和 `source-manifest.json` SHA-256，并同步写入批次、状态和图片资产契约。创建失败不会提前改变资产契约；任务启动后切换全局模式不会改变本批次。
- 手动商品结束于 `WAITING_MANUAL_REVIEW`，批次和结果统一为 `AWAITING_MANUAL_UPLOAD / 等待人工检查`，未真正上传前不计入成功/已上架数量。
- `output/accepted-images/manifest.json` 记录每张确认图的槽位、SKU/共享角色、候选与确认路径、SHA-256、确认人/时间及设计版本。替换、重生、删除、设计变化或文件被覆盖都会撤销确认；上传前必须重新核对当前采集/设计版本和完整N+8槽位。
- SQLite连接生命周期已修复并用 `ResourceWarning` 提升为错误执行全量测试。当前主线全量结果：462项，全部通过，126项明确跳过，0失败、0错误、0资源泄漏警告。
- 离线项目自检当前为PASS：0错误、1项透明警告。P000001-P000005按归档审计数据处理；P000006被识别为旧目录契约商品，已禁止进入新批次，等待用户后续决定迁移或归档，不会被伪装成新契约商品。
- 本地浏览器已验证：P900001不出现在工作台，原始/候选/拒绝/确认四类图片分区分别渲染，未发现控制台错误；新采集商品的接受/拒绝/替换/重生交互仍留给下一次真实端到端。另修复了通知接口读取旧JSON导致“需要处理1但页面为空”的状态不一致。
- 本轮没有启动真实商品或图片任务；Ozon CREATE/UPDATE 0，Ozon 只读回查 0，库存接口 0。

### 仍待一件全新正式采集商品完成的真实验收

- 需要用户在1688通过当前工作台插件新采集一件商品，并亲自锁定SKU、最终类目和店铺。现有P000006、P900001、P900002和对话附件都不能代替该输入。
- 新输入到达后只运行“不调用Ozon”的链路，验证联网Codex设计、俄文SEO/30标签/属性、N+8逐图提示词、实际生图与排版、四类素材区、接受/拒绝/替换/重生和最终`WAITING_MANUAL_REVIEW`。未确认完整N+8时上传按钮必须硬阻断。

## 2026-07-16 俄文资料与电商图片专项重构（当前任务）

- 目标已从“生成若干图片”收敛为固定生产链：采集事实与SKU/类目 → 商品分析 → 俄文SEO资料与属性 → `ecommerce-creative-brief.json` → 全部逐图提示词 → 每SKU一张主图 → 正好8张共享详情图 → 技术硬质检 → 预览。
- 新增`scripts/ecommerce_creative_brief.py`和`templates/ecommerce-creative-brief.schema.json`。Brief把买家、场景、卖点优先级、商品专属视觉风格、每张图的商业目的、俄文文案、真实原图引用和不可改变项一次性传给图片规划。
- 图片规划不再只按风格模板拼槽位；`image-plan.json`现在引用Brief，运行时严格要求8张详情图。结构/尺寸/SKU/包装类证据图走`compose_from_real_images`，场景图走`edit_real_image`。
- 俄文生成提示已改为“商品理解后按俄罗斯Ozon搜索语序生成SEO资料”，禁止中文直译、虚构热度和把无关词堆成标签；图片生成必须先读Brief和完整文字资料。
- 本轮只做离线代码、规则和验收样品，不启动真实商品任务，不调用Ozon写接口、只读接口或库存接口；已归档任务和P000006不重新提交。
- 更正：2026-07-12原始`ozon_pre_generation_master_prompt.md`和8张截图已经找到并复制到项目 reference 目录；只作手动上架的信息结构和质量基准，不作商品参考图。
- 整改：旧`P900001`与手工食品盒基准不是同一商品，且输出曾由离线脚本硬编码/占位渲染，不能证明真实链路；已迁至`test-data/offline-audit/P900001`。新的P900002只属于`test-data`开发样板，禁止进入正式生产或上传。
- 定向专项测试：新增创意Brief和离线验收测试全部通过；图片工作流、主图策略、历史PASS硬阻断、图片主机恢复和上传安全门禁通过。全量回归共421项：355通过、22失败、19错误、25跳过；失败/错误集中在旧归档商品P000003/P000004/P000005/P000014历史夹具与旧断言，本专项未新增Ozon调用。

## 2026-07-16 P000006 Ozon商品卡重复内容错误修复（当前权威）

- 用户确认三家店铺都出现同一问题，且此前不止一次发生；本次按系统性缺陷处理，不按单商品偶发问题处理。
- 已核对三家店铺实际提交快照：`Комплектация`被错误填入中文取证说明和本地图片路径；`Объем, л`又把SKU中的自重/实装“斤数”误算成毫升文本，分别触发“商品卡含不允许字符/汉字”和“请用数字填写属性”。
- 容量解析已修复：明确的`2.5升/5升/10升/25升`优先于自重、承重和实装斤数；Ozon Decimal属性只生成`2.5/5/10/25`这类纯数字。仅在没有明确体积且确属“斤装”容量时保留旧的可追踪估算规则。
- 配件字段已修复：中文事实和图片路径只留在本地证据中，Ozon可见字段仅输出有明确证据的简洁俄文；P000006为`внутренняя крышка, внешняя винтовая крышка`。
- 上传器新增远端可见内容硬门禁：标题/属性含中文、控制字符、本地图片路径，或Decimal/Integer属性不是纯数字时，任何CREATE/UPDATE前直接阻断并显示中文原因。
- P000006本地修正清单已写入`output/ozon-card-correction.json`；三家店铺已提交的历史payload和task_id保留作审计，没有自动重试、没有自动UPDATE、没有调用库存接口。远端商品卡由用户在Ozon后台按修正清单处理。
- 隔离副本预演结果：4个SKU容量为`10/25/5/2.5`，配件字段为纯俄文，新增内容门禁0阻断；相关定向测试7项通过（2项缺少可选运行夹具而跳过）。

## 2026-07-16 共享工作台与自动设备识别（当前最高优先级规则）

- 用户已明确取消“成员访问码 + 每人商品隔离”。同一工作室局域网内的电脑不再填写访问码，打开主电脑工作台即可使用。
- 商品、批次、预览检查、失败处理、运行日志与上架前确认对所有工作室电脑共享；旧`owner.json`和批次owner sidecar只保留为历史采集来源，不再用于过滤。
- 后台按局域网电脑自动识别并记录操作设备；写操作只记录时间、接口、设备和结果，不记录请求正文、店铺密钥或其他敏感内容。
- 主电脑保留店铺凭证、自动模式、系统设置与安全退出权限；其他电脑可采集、查看、修改、运行、重试和进行最终人工确认，但不能修改店铺与系统配置。
- 安全边界仍保留：只允许配置的私有局域网地址；工作台同源页面和1688采集插件可访问，任意外部网页不能借局域网免码调用接口。
- Edge插件升级为`0.4.11`：只填写主电脑地址，自动生成设备标识并连接，不再保存或发送成员访问码。旧插件仍可临时连接，但其他电脑应更新至0.4.11以获得稳定设备留痕。
- 本次只改变本地工作台可见性、权限与设备记录，没有触发Ozon CREATE/UPDATE、远端回查或库存接口。

下一步：在123电脑刷新工作台，确认能看到共享商品与预览入口；随后更新该电脑插件到0.4.11，再继续当前商品的人工检查与生图恢复。

## 2026-07-16 P0阻断修复：P000006 SKU图片完整采集（当前权威）

- P000006根因已确认：1688页面25个SKU只渲染了21个SKU图片，旧插件仍允许选择缺图SKU，最终在78%阶段停住。本轮只修复采集链路，未重新上传P000006，未调用Ozon。
- Edge插件已升级至0.4.9：采集/预览前自动遍历SKU列表、滚动触发懒加载并重新提取图片；缺任一SKU图片时在进入选择前阻断，缺图SKU不可勾选，并显示`SKU图片：已加载/总数`。
- 本地服务增加二次硬门禁：已选SKU缺图，或新插件的全量`sku_image_preflight`不是PASS，均返回中文`SKU_IMAGES_INCOMPLETE`，不会创建商品目录，也不会让旧PASS检查文件绕过当前检查。
- 新插件包：`release/1688商品采集插件-0.4.9.zip`。其他电脑必须用此包替换旧0.4.8并在Edge扩展页重新加载。
- 验证结果：插件/后端定向测试32项全部通过，JavaScript语法检查通过，插件包内容和版本校验通过；Ozon写入、只读回查、库存调用均为0。

下一步：在其他电脑更新插件后，打开1688商品页等待SKU图片状态变为完整，再重新采集P000006或新商品；缺图时必须先修复页面加载，不允许绕过阻断。

## 2026-07-16 Mac启动器与生图主机自恢复（当前运行入口）

- 桌面入口已经安装为`/Users/apple/Desktop/AI Factory.app`；双击后会复用现有服务或启动后台守护服务，并直接打开`http://127.0.0.1:8765/workbench`。关闭浏览器不会停止AI Factory。
- 工作台顶部新增真实主机状态：`主机正常 / 主机恢复中 / 主机需要处理`，并新增`安全退出`。安全退出会先等待当前步骤落盘，再停止服务守护；下次双击桌面App可以继续。
- 生图步骤以5分钟“没有新图片且没有进度心跳”为卡死标准。首次卡死会终止本商品的Codex生图进程，保留已完成图片，只重试缺失图片；同一轮最多自动恢复1次。
- 同时只处理1个商品的生图，但单商品内部最多3张图并发。当前商品进入恢复等待时会回到队列尾部，不阻塞其他商品继续处理。
- 第二次恢复仍失败时，不再无限重启：工作台显示具体原因和一键继续入口，并发送Mac通知；不会删除已经生成成功的图片。
- 服务或Mac意外中断后，只恢复已获得运行授权且仍处于`PROCESSING/QUEUED`的未完成商品，不会恢复已归档旧任务，也不会自动触发Ozon上传。
- 工作台默认恢复为`手动检查`模式：运行前确认类目、SKU、店铺等信息，生成完成后仍由用户最终确认上传。`自动模式`保留为负责人明确开启的可选项。
- 本轮没有调用Ozon CREATE、UPDATE、只读回查或库存接口，没有启动任何真实商品上传。
- 定向回归79项全部通过；完整回归398项为25失败、19错误、25跳过，与修改前基线的失败/错误/跳过数量完全一致。既有失败仍来自归档旧商品夹具、旧价格/类目断言和插件发布包预期，本轮没有新增回归。

下一步：只采集1件全新真实商品，先验证`选择类目/SKU/店铺 -> 手动确认 -> 资料与生图断点恢复`；到最终上传确认页停止，由用户决定是否提交。多店同步、类目属性人工修正和售价公式的一次性改价继续按后续阶段验证。

## 2026-07-16 旧任务本地归档与远端回查停用（当前权威规则）

- 用户已确认Ozon后台旧商品已归档；本地`P000001`–`P000005`及全部历史批次统一进入7天恢复区，原因固定为`user_archived_in_ozon`。
- SQLite中5个商品为`ARCHIVED`，8条带task_id的历史任务为`ABANDONED`；`task_id`、`offer_id`、已有`ozon_product_id`、`payload_hash`、写入次数和历史回查次数均保留，不重置、不重传。
- 历史批次共25个，均为本地`ARCHIVED`；完整商品目录、图片和批次文件暂不删除，`cleanup_after=2026-07-23`，恢复清理前仍可读。
- `remote_status_worker`和图片状态远端监视器不再自动启动；手工刷新入口只返回“远端回查已停用”，不会调用Ozon。
- P000004图片通道PID `13003`（caffeinate `13004`）和P000005图片通道PID `89591`（caffeinate `89592`）已通过stop-file正常退出，未使用强制终止；当前真实图片进程0、回查进程0。
- 以后Ozon写请求返回有效`task_id`即进入本地终态`HANDED_OFF_TO_OZON`，界面显示“已提交Ozon，后续请在Ozon商品卡后台处理”，不再等待商品ID、不做自动回查或上架后UPDATE管理。
- 临时图片通道采用固定24小时TTL，TTL独立于远端状态；提交后不因回查关闭，期满自动结束。
- 本轮Ozon CREATE、UPDATE、只读回查、库存接口调用均为0；没有启动新商品或真实30商品试跑。
- P0定向回归：23项通过；全量`unittest discover -s tests -p 'test*.py'`：394项运行，325通过、25失败、19错误、25跳过。失败/错误集中在已归档后仍引用P000004/P000005/P000014的旧历史夹具、旧价格/类目预期和插件发布包版本，不是本轮归档、停回查或本地交接测试失败。
- 当前服务`/health`正常；Python编译、状态Schema校验、SQLite完整性和权限检查通过。

## 当前目标

在已建立的本地Git基线之上，用一件新采集的真实商品验证完整主流程：

`1688选择SKU和最终Ozon类目 -> 我的采集箱 -> 运行任务 -> 资料/价格/图片生成 -> 多店提交 -> 本地交接完成（Ozon商品卡后台继续处理）`

旧商品已由用户在Ozon后台归档；本地资料进入7天恢复区，不立即物理删除，也不再回查或重传。

## 2026-07-15 财务自动同步重试风暴已止损并修复

- 23:00后共确认77条`scheduled_catch_up`失败记录，均为只读Finance接口`/v3/finance/transaction/list`返回HTTP 400；旧调度器因当天没有整批成功，每分钟重新触发一次。
- 修复前已创建`0600`数据库恢复点：`runtime/finance/backups/20260715-234809-before-finance-retry-guard.sqlite3`，完整性检查为`ok`。
- 计划任务状态和Ozon只读熔断状态现持久化到SQLite；重启服务不会丢失退避状态。HTTP 400等不可重试4xx当天停止；429/5xx/网络错误至少退避1小时，整批最多自动尝试2次。
- 一家店铺触发Ozon只读错误后，当前批次剩余店铺直接标记`skipped`，不再继续请求；手动同步在保护期内也会被本地拦截。
- 90天Finance重扫从原31天（含首尾）窗口改成最多28天，当前拆分为4个安全窗口，避免超过接口单次最长周期。
- 本地调度检查由60秒改为300秒；2026-07-15保护状态为`blocked_for_day`，最早下一次自动尝试为2026-07-16 15:00。
- 部署后服务已恢复到`0.0.0.0:8765`；失败记录仍为77、运行中同步0、人工保护验证没有新增同步记录。商品图片通道未被终止。
- 财务专项18项和财务/工作台重点回归79项全部通过；Python/JavaScript语法、SQLite完整性与文件权限检查通过。Ozon商品写调用0，库存调用0；修复验证没有真实调用Ozon。

## 2026-07-14 商品卖点到图片链路修复完成

### 3张本地成品基准已完成

- `P000001`已经生成3张可直接评审的成品基准：科技感主图、厨房卖点图、便携/办公场景图；全部为3:4，全部带准确俄文标题和短卖点。
- 成品不再出现空白占位框，也不会把无字底图当结果；旧图上残留的文字会先被完整替换，避免新旧两层文案重叠。
- 文字合成支持“主标题 + 副卖点”两级排版和顶部/底部强制落位，保证俄文准确，不再依赖生图模型随机拼写。
- 本地目录：`products/P000001/output/image-quality-benchmark-v1/`。当前只建立质量标准，没有覆盖Ozon现有图片，也没有重新上传。
- 本次追加回归：`tests.test_image_workflow_fix`共7项全部通过。Ozon CREATE/UPDATE/只读查询均0次，库存接口0次。

- 已修复根因：原先`product-analysis.json`虽有真实卖点，但`product-positioning.json`只取第一条，并把买家、痛点、场景、情绪和视觉方向保留为`unknown`；图片规划又从长描述随机抽一句，导致图片有商品但没有清晰购买理由。
- 产品定位阶段现在自动生成3至5条可追溯买家卖点，并同时生成目标买家、购买痛点、使用场景、情绪触发和商品专属视觉方向。事实不足时用类目、商品类型和SKU差异形成低风险`supported_inference`，不虚构功率、认证、承重、材质、功能或配件。
- 按用户最新要求取消“卖点不足就停止生图”的门禁：批次不能因此暂停。专用俄文图片文案缺失时会从已生成标题、卖点和场景自动压缩为短句继续执行；禁止把`unknown`或无字底图当成成品。
- 俄文文案增加`image_copy_ru`：覆盖主图、利益、问题解决、场景、功能、细节、使用、对比和提示图，每张1至3句、每句不超过70字符；`main_by_sku`逐SKU表达容量、材质、颜色或配置差异。
- 当前`P000001`已验证：定位状态`completed`，5条买家卖点；新图片计划为2张SKU主图+8张共享详情图共10张，全部图片都有非`unknown`俄文卖点。350ml主图使用316S，400ml主图使用304S和配盖说明，没有串SKU。
- 完整自动测试：371项`OK`，114项条件跳过，0失败。项目虚拟环境补齐了已在`requirements.txt`声明但未安装的FastAPI/Uvicorn测试依赖。
- 本轮没有覆盖当前已上传图片、没有重新执行上传、没有调用Ozon接口。CREATE 0次、UPDATE 0次、只读查询0次、库存接口0次。

本阶段修改文件：`scripts/product_positioning_agent.py`、`scripts/image_planner.py`、`scripts/marketplace_content_generator.py`、`scripts/run_batch.py`、`scripts/validate_product.py`、`templates/product-positioning.schema.json`、`templates/copy-ru.schema.json`、`templates/image-plan.schema.json`、相关图片/定位测试，以及`P000001`的本地定位与俄文图片短文案文件。

下一步：用户只需判断这3张的视觉方向是否达到基准；认可后将同一“先分析卖点、再生成场景、最后准确叠字”的规则用于下一件真实采集商品，不对`P000001`重复CREATE。

## 2026-07-14 全项目自检与安全修复完成

- 新增离线自检入口：`python3 scripts/project_self_check.py --full-tests`。它会检查关键文件、Python/JavaScript/JSON语法、硬编码凭证、外部AI客户端、库存接口、插件源码镜像与离线类目缓存、商品/批次/队列状态、商品Schema和完整测试；自检本身不联网。
- 最新结果：16项检查全部通过，0失败、0警告；完整自动测试371项`OK`，114项条件跳过；`P000001`结构校验通过。
- 修复商品图片路径越权：工作台只能读取、替换、删除和导出当前商品`output/images`或`output/generated-images`内的图片，污染的图片规划不能再触碰README、环境配置或其他商品文件。
- 修复1688图片下载风险：只允许可信的1688/阿里图片域名，逐次校验重定向，限制协议、端口、类型和25MB体积，阻止本机、局域网和伪造域名请求。
- 修复Ozon待回查状态：`PENDING_REMOTE`显示99%且不写完成时间；只有真正`UPLOADED`才显示100%，同时清除已经提交后仍残留的“等待确认上传”提示。
- 修复Edge插件未捕获的`Failed to fetch`：类目最近/收藏、收藏当前类目、打开工作台和插件初始化失败时均显示中文错误，不再产生黄色未处理异常；运行文件与TypeScript镜像保持一致。
- 新建成员访问码改用PBKDF2-SHA256（20万次迭代+随机盐）；旧SHA256访问码继续兼容，避免现有成员失效。
- Edge插件版本已统一为`0.4.7`，内置7424个中文最终类目和7424份规则缓存；最新安装包为`release/1688商品采集插件-0.4.7.zip`，压缩包校验通过。
- 工作台已从旧服务进程平稳重启；`/health`和`/workbench`返回正常。真实商品`P000001`为`UPLOADED / 100%`，完成时间存在，2张SKU主图和8张通用详情图共10张均返回200。
- 本轮未调用任何外部接口：Ozon CREATE 0次、UPDATE 0次、只读查询0次、库存接口0次；没有改动当前商品、店铺、队列或选品关键词数据。

本轮直接修改文件：`collector/local-ingest/app.py`、`scripts/image_source_preflight.py`、`scripts/multi_store_upload.py`、`scripts/workbench_operators.py`、`scripts/project_self_check.py`、Edge插件的`content/popup`运行文件与源码镜像、插件`package.json`及对应自动测试。

当前剩余事项：114项跳过测试依赖已删除的旧历史商品/可选运行夹具，不属于本次失败，但后续应逐步改造成独立测试夹具；插件目录尚未安装本地TypeScript编译器，当前以运行JS语法、源码镜像和完整测试保证一致；项目根`AGENTS.md`仍保留第一阶段“不开发UI/插件/API”的旧描述，与当前实际范围冲突，需由项目负责人另行确认后统一规范。

## 2026-07-14 最新权威状态：首件真实商品已提交成功

本节覆盖本文后面保留的“等待上传”旧快照。

- 用户已明确授权将`P000001`上传到店铺`zhonglian1`；上传前只读查重确认两个`offer_id`均不存在，因此执行CREATE，不是UPDATE。
- 第一次执行在写入前被本地安全门拦截，原因是Rich Content引用SKU主图，而上传器错误排除了`variant_main`图片；该次Ozon写请求为0。
- 已修复SKU主图Rich Content解析并增加回归测试；安全重试时把两个独立商品卡放在同一个`/v3/product/import`请求中，Ozon商品写接口总计1次。
- Ozon任务号：`5081018919`。
- SKU `5931524204563`：`offer_id=P000001-5931524204563`，Ozon Product ID `5482520711`。
- SKU `5931524204569`：`offer_id=P000001-5931524204569`，Ozon Product ID `5482520644`。
- 只读异步回查结果：两张卡均为`success`，店铺状态`SUCCESS`，商品状态`UPLOADED / 100%`，远端错误0。
- Ozon最终回查已确认两张卡均为`moderate_status=approved / validation_status=success / Готов к продаже`；每张卡为1张独立主图+8张通用详情图，9张图片全部进入Ozon官方CDN。
- 实际请求包含2个商品项；递归检查未发现`stock`、`stocks`、`warehouse_id`或`quantity`字段。库存接口调用0次。
- 已修复多店聚合状态遗漏：主商品状态现在同步保存首个`offer_id/product_id/task_id`、`ozon.upload_status=uploaded`以及合法的`OZON_READY -> UPLOADING -> PENDING_REMOTE -> UPLOADED`历史。
- 当前批次`B-846B65169489`已同步为`COMPLETED / 100%`，不再显示“等待手动上传”。
- 最新完整自动测试：368项`OK`，114项条件跳过，0失败；`scripts/validate_product.py products/P000001`通过；工作台健康检查200。

Ozon当前说明为`Нет на складе（无库存）`，这符合项目禁止提交库存的规则；用户以后需要在Ozon后台自行添加库存才会真正可购买。该商品禁止再次CREATE，后续资料修改只能走安全UPDATE。

本阶段修改文件：`ozon-uploader/ozon_uploader/service.py`、`scripts/multi_store_upload.py`、`tests/test_stage42_ozon_uploader.py`、`tests/test_multi_store_upload.py`、`products/P000001/status.json`、`products/P000001/output/store-publications.json`、`products/P000001/output/store-runs/zhonglian1/*`、`batches/B-846B65169489/*`、`CURRENT_TASK.md`和`PROJECT_HANDOFF.md`。

## 2026-07-14 最新权威状态：SKU图片组与容量变体修复完成

本节覆盖本文后面保留的旧运行快照。

- 工作台现在按实际选中SKU展示图片：采集几个SKU就显示几个图片组；每组固定为“该SKU专属主图 + 同一套通用详情图”。通用详情图只重复展示，不重复生成文件。
- `P000001`已验证为2个图片组：350 ml主图、400 ml主图分别对应正确SKU，每组共享同一套8张详情图。
- Ozon分组继续严格读取类目规则快照的`is_aspect`：有官方容量变体字段时，同卡上传并给每个SKU写入自己的官方容量值；没有官方容量变体字段时，不虚构容量字段，改为独立商品卡。
- 已修复“安全拆卡被错误当成上传失败”：合法`separate_cards`不再进入`production_blockers`，每张独立卡使用独立型号标识和SKU专属名称，避免被Ozon再次误合并。
- 本地真实商品干跑结果：2张独立卡、2个独立`offer_id`、各自正确专属主图、共享8张详情图；`production_blockers=[]`、`api_writes_performed=false`、库存字段0。
- 已清理旧失败残留：商品保持`OZON_READY / 95% / manual_ozon_upload`，店铺记录恢复为`SELECTED / not_submitted`，旧“安全停止”和旧拆卡错误不再影响当前任务。
- 最新完整测试：359项全部`OK`，114项按缺少可选历史夹具条件跳过，无失败。
- 本轮Ozon CREATE 0次、UPDATE 0次、其他写接口0次、只读接口0次、库存接口0次。

当前尚未完成：真实Ozon提交和异步回查。下一步先刷新工作台确认两组SKU图片显示正确；只有用户主动点击最终上传按钮后，才执行一次安全CREATE或明确存在时的UPDATE。

本阶段修改文件：`collector/local-ingest/app.py`、`collector/local-ingest/static/workbench.html`、`collector/local-ingest/static/workbench.js`、`collector/local-ingest/static/workbench.css`、`variant-compatibility-checker/variant_compatibility_checker/service.py`、`ozon-uploader/ozon_uploader/service.py`、相关SKU图片/变体/工作台测试，以及`P000001`本地状态和店铺发布记录。

## 2026-07-14 当前权威状态：等待手动确认上传

- `P000001`的资料、价格、属性和10张图片已经完成，图片硬检查覆盖10/10，图片质检结果为`pass`，最终上传检查为`upload_allowed=true`。
- 商品当前为`OZON_READY / 95% / next_action=manual_ozon_upload`；这不是卡在78%，也不是仍受旧“安全停止”影响。
- 当前批次`B-2261A4514AF3`为手动模式，因此正确状态是`AWAITING_MANUAL_UPLOAD / 95%`，不是“已完成100%”。用户仍需在预览检查页点击最后一次“确认修改并立即上传”。
- 已修复批次运行器中手动模式分支顺序错误，以及批次结束时无条件写成`COMPLETED / 100%`的问题。
- 工作台真实页面已验证：采集箱显示“待确认上传 · 上架前最终检查 · 95%”，商品主按钮显示“确认并上传”，批次入口显示“检查并上传”。
- 本轮没有点击最终上传按钮，没有调用Ozon接口；Ozon CREATE 0次、UPDATE 0次、其他商品写接口0次、库存接口0次。
- 最新完整自动测试：356项全部`OK`，其中114项按缺少可选运行环境/历史夹具条件跳过，无失败。
- 工作台服务当前运行在`0.0.0.0:8765`，本机健康检查返回200。
- 下一步唯一业务动作：用户检查当前成品后主动确认上传；提交后只做异步状态回查，pending或状态不明确时不重传。

## 2026-07-14 工作台断线修复

- 根因：上一轮由Codex临时进程启动的工作台会在任务结束后被系统回收；旧启动器又要求服务终端始终保持开启，因此浏览器出现`127.0.0.1拒绝连接`。
- 已将`启动工作室工作台.command`改为真正的后台启动，并由`scripts/run_workbench_service.sh`持续守护；终端可以关闭，Uvicorn异常退出后2秒自动重启。
- 当前后台守护进程PID为`82667`，父进程为系统PID 1；实际服务进程PID为`82669`，正在监听`0.0.0.0:8765`。
- 已通过主电脑真实Terminal入口启动，不依赖Codex执行会话；浏览器已重新请求`/workbench`并返回200。
- 最新完整自动测试：357项全部`OK`，114项按条件跳过，无失败。
- 本轮仍未调用任何Ozon接口；商品继续保持`OZON_READY / 95% / manual_ozon_upload`。

## 2026-07-13 最新流程优化：资料优先、最后生图

- 商品只做一次事实分析；不再为了填写属性和生图反复分析同一商品。
- 新执行顺序为：`商品定位 -> 俄文基础文案 -> 标题/简介/30标签 -> SKU/价格/全部类目属性 -> 图片风格 -> 一次生成全部逐图提示词 -> 最后统一生图 -> 图片质检 -> 上架前最终检查 -> Ozon上传`。
- 图片规划必须读取最终标题、简介、30个标签、最终属性、SKU分组和最终价格；这些资料不完整时禁止开始生图。
- 所有逐图提示词必须先完整生成并保存在`output/image-plan.json`，之后才允许逐张生图；生图阶段不得再次分析商品、猜类目或重填属性。
- Ozon Rich Content和最终上传检查延后到图片完成后执行，但复用前面已生成的资料，不重复做商品分析。
- 图片提示词已增加硬限制：禁止空白圆角框、空文字面板、占位卡片和通用模板图；文字留白必须来自真实构图需要。
- 已修复`keywords-ru.json`中`source_refs/evidence`字段不兼容导致的真实任务失败。
- 已清除`P000001`上一轮不达标的生成图、图片锁和旧图片质检结果；1688原始图和原始输入全部保留。

## 本阶段已完成

- 已确认项目根目录并建立本地Git仓库，分支为`main`。
- 基线提交：`a8f8b58df5f3e06f7f924e45b348a555d3fc0375`。
- 基线标签：`baseline-before-category-selection`。
- 工作台与成员隔离阶段提交：`a6491560c3033af5b41153b11d89437189774e81`。
- 清理旧采集箱和重复发布镜像提交：`7d54daf`。
- 一键启动、类目搜索和7424类目离线规则提交：`aadf80b`。
- 未配置远程仓库，未上传GitHub。
- 已清空`products/`、`batches/`、`runtime/`、运行日志、图片缓存、任务队列和旧发布包，仅保留目录占位文件。
- 已保留真实店铺配置、店铺密钥本地文件、局域网访问配置、Ozon本地类目树、属性规则、字典值和缓存。
- 工作台主入口已收敛为：`我的采集箱 / 需要我处理 / 已上架商品`。
- 商品卡只显示当前一个主要动作；运行、回答问题、修复、检查上传、查看状态按商品状态自动切换。
- 已实现成员级数据隔离：每个成员只能看、改、运行、删除和上传自己采集的商品。
- 负责人只能管理成员、店铺和全局设置，也不能查看其他成员商品。
- Edge插件提交的成员访问码决定商品归属；成员可选择任意已配置店铺。
- 产品、批次、通知、导出和发布记录均由后端按当前成员过滤。
- 已接入电脑通知入口；关键问题、明确失败和待上传事项只通知商品所属成员。
- 只在商品身份、SKU映射、变体、颜色、结构、配件数量等关键事实不明确时提问；普通可选属性不打断任务。
- 已接入整组图片风格意见和单图意见；简单描述如“更明亮”“更科技感”可进入视觉规划。
- 图片风格意见不能覆盖真实商品结构、颜色、SKU差异和配件数量。
- 图片步骤支持输入哈希复用；风格未变时复用结果，风格变化时从风格选择阶段失效，不重跑无关前序步骤。
- Edge插件升级到`0.4.7`；类目搜索支持输入即时反馈、搜索按钮和回车触发。
- 7424个最终类目的采集必需规则已压缩为一份主程序与插件共用的本地离线缓存；选择类目不再临时查询Ozon，也不保留重复缓存副本。
- 离线缓存覆盖必填属性与`is_aspect`；官方批量文件没有提供的字典值保持`unknown`，插件会保留最近12个已加载类目的完整规则和字典值逐步补全。
- 工作台已在真实浏览器验证电脑端1280×720和手机端390×844，页面无控制台错误或警告。
- 已删除独立旧`/inbox`页面，商品操作统一进入当前工作台；后端`/api/inbox/*`数据接口继续保留供工作台使用。
- 已删除`release/edge-extension/`重复源码镜像，发布ZIP改为直接从`collector/edge-extension/`唯一源码生成。
- 已阻止外部网页通过跨站请求访问本机回环地址并自动继承负责人权限；本机工作台和现有Edge插件兼容方式不变。
- 已修正局域网使用说明：去除不存在的启动器说明，并明确成员商品互相隔离。
- 已新增根目录`启动工作室工作台.command`：双击即可启动、自动打开浏览器、重复点击不会重复启动，并使用相对项目路径而非开发机绝对路径。

## 当前真实数据状态

- 商品：1，当前真实测试商品`P000001`，2个SKU，类目已锁定为`category_id=17039627 / type_id=94982`。
- 历史批次：3；当前没有运行中的批次或后台图片任务。
- 运行队列：0。
- 当前步骤：`marketplace_content`，商品已安全停止，工作台显示60%；下一次点击“运行任务”会按新的资料优先顺序从此断点继续。
- 当前商品已生成可验证的最终标题、简介、30个标签、2个SKU价格、43项最终类目字段记录和10份逐图提示词；其中14项有可追溯值、29项按真实性规则标记`unknown`，3项必填属性已全部填写。
- 当前生成图片数量为0；工作台的10个图片位均为“等待生成”，不会复用上一轮不达标图片。
- 店铺`zhonglian1`：配置保留、已启用、凭证已配置、上次只读验证状态保留。
- Ozon商品写接口调用：0次。
- Ozon库存接口调用：0次。
- 当前没有pending、task_id或远端商品需要回查。
- 工作台服务当前已由一键启动器运行在`0.0.0.0:8765`，本机健康检查返回200。

## 最近测试结果

- 2026-07-13资料优先流程改造后完整自动测试：345项，结果`OK`；230项实际执行通过，115项历史运行数据测试跳过。
- 新增覆盖：旧图片优先断点自动迁移、预生图字段补全、最终上传检查后移、最终资料驱动图片提示词、禁止空白占位面板、关键词证据字段兼容。
- 真实工作台API验证：`P000001`状态为`STOPPED`、进度60%、下一步`marketplace_content`；10个图片位均有商品专属提示词且没有生成文件。

- 2026-07-13本轮完整自动测试：343项，结果`OK`；228项执行通过，115项历史运行数据测试跳过。
- 本轮新增验证覆盖：失败商品正确归入“处理失败”、不重复要求整批确认、从失败步骤继续、工作台卡片/进度入口、便携厨房电器图片风格、Codex子任务固定使用项目Python环境。
- 真实浏览器已验证：商品卡显示“生成中 · 生成商品图片 · 71%”；点击“查看进度”能进入预览检查，并显示“已完成x/10张”以及每张图的完成/生成中/等待状态。
- 只包含本轮一键启动、类目搜索和离线缓存修改的干净临时副本：323项，结果`OK`；203项实际执行通过，120项跳过。
- 当前整个工作区自动发现339项，结果`OK`；219项实际执行通过，120项跳过。
- 跳过项是依赖已按用户要求删除的旧真实商品目录的历史集成测试；没有恢复旧商品数据。
- 新增的成员隔离、负责人权限、关键问题、视觉意见、工作台主入口和插件版本测试均已执行并通过。
- Python语法检查通过。
- 浏览器真实页面验证通过，控制台错误/警告0。
- Edge插件`0.4.7`发布ZIP已重新生成并通过完整性检查；包含7424类目离线规则，大小约1.6 MiB，SHA-256为`97c372007e967c3ace621a27e0c21fb31e67db1002d6a34110971566a8d16602`，重复打包哈希一致。

## 当前未完成

- `P000001`尚未按新流程生成第一组图片，也未完成第一件真实可售商品闭环。
- 下一次由用户点击“运行任务”后，将先快速复用/补齐商品资料，再进入耗时最大的生图阶段。
- 120项历史集成测试尚未改造成完全独立的合成夹具；当前默认测试虽为绿色，但测试体系仍需去除对旧运行数据的历史依赖。
- 真实多店CREATE/UPDATE和逐店异步回查仍需在新商品上验收。
- 图片质量仍需用新商品与用户手动流程的结果做一次真实对照，确认视觉效果而不仅是结构检查。
- 对外商业化前仍需30个真实商品、至少5个类目的工作室试跑。
- 工作台前端仍有不可达的旧首页、Skill、经验和高级日志渲染代码；因选品与关键词模块正在另一对话并行修改同一组前端文件，本轮暂不触碰，待对方完成后再单独清理冲突范围。

## 下一步直接动作

1. 用户在工作台对`P000001`点击一次“运行任务”，验证新的资料优先流水线。
2. 观察10张新图逐张生成；失败时只从失败单图断点重试，不重复标题、价格、SKU和属性步骤。
3. 上传前执行本地预检和只读远端存在检查；状态明确后再由用户主动运行真实上传。
4. 一旦提交，逐店逐SKU保存`offer_id/task_id/product_id/payload_hash`并回查到明确结果。
5. 将115项旧数据依赖测试逐步改造成自带夹具，再开始30商品工作室试跑。

## 2026-07-13 商品处理无响应修复

- 原因不是工作台服务停止，而是该类目没有匹配到可用图片风格，任务以`FAILED_HARD_BLOCKER`停在`image_plan`；旧页面又把它显示成“需要人工审核”，点击区域也不明确。
- 已增加便携式厨房小家电专属视觉风格`portable_kitchen_appliance_fresh`，当前商品分类置信度0.94；不使用白底或固定厨房模板。
- 失败商品现在显示明确失败步骤、百分比和错误原因；整张商品卡及主要按钮均可进入详情。
- 已授权失败任务从断点继续，不再重复要求用户进行第二次批次确认。
- 运行中的详情页显示当前步骤、已完成图片数量、逐图状态和安全停止按钮。
- 子任务Python环境固定到项目`.venv/bin/python`，避免本机其他Python缺少Pillow导致点击后看似卡住。
- 已完成的图片文件和硬检查结果会被保留，不会因重试重复生图。
- 本轮Ozon写接口0次，库存接口0次。

## 关键限制

- 不提交库存字段，不设置`stock=0`，不调用库存接口。
- pending、上传中、审核中或状态不明确时禁止重传。
- 已成功CREATE不得重复CREATE；明确存在的商品只能安全UPDATE。
- 单商品最多10个SKU；单商品或单店失败不能阻塞整个批次。
- 类目必须在1688采集时由用户最终选择，运行中不得重新猜测或替换。
- 必填属性、字典值和变体分组必须读取该类目的本地规则快照及`is_aspect`。
- 不虚构材质、承重、认证、功能和配件；尺寸、重量估算必须带`estimated`和置信度。
- 原始事实、估算、人工修改和Ozon字段分层保存；不得覆盖原始图片和原始输入。

## 本阶段主要修改文件

- `.gitignore`
- `config/operators.example.json`
- `scripts/workbench_operators.py`
- `collector/local-ingest/app.py`
- `collector/local-ingest/static/workbench.html`
- `collector/local-ingest/static/workbench.js`
- `collector/local-ingest/static/workbench.css`
- `scripts/pipeline_runtime.py`
- `scripts/style_selector.py`
- `scripts/image_planner.py`
- `scripts/pipeline_observability.py`
- `scripts/run_batch.py`
- `rules/style_selector_rules.json`
- `rules/style_profiles.json`
- `rules/image_structure_rules.json`
- `tests/test_batch_confirmation.py`
- `tests/test_image_workflow_fix.py`
- `tests/test_stage3_style_selector.py`
- `tests/test_workbench.py`
- `collector/edge-extension/manifest.json`
- `collector/edge-extension/package.json`
- `collector/edge-extension/popup.html`
- 删除`release/edge-extension/`重复源码镜像
- `scripts/package_edge_extension.py`
- `tests/test_edge_extension_packaging.py`
- `tests/test_collection_inbox.py`
- `tests/test_lan_collaboration.py`
- `docs/工作室局域网使用说明.md`
- `tests/test_workbench_operators.py`
- `tests/test_workbench_ownership.py`
- 相关工作台、图片、类目和历史数据依赖测试
- `README.md`
- `WORKBENCH_REQUIREMENTS_MATRIX.md`
- `PROJECT_HANDOFF.md`
- `CURRENT_TASK.md`

## Git状态说明

- 当前仓库仅为本地仓库，没有remote。
- 基线提交和标签已完成。
- 本阶段实现已提交为`a6491560c3033af5b41153b11d89437189774e81`，纯净提交副本318项测试通过。
- 工作区还有另一对话正在修改的选品与关键词文件，本轮不修改、不删除、不纳入本轮提交。

## 2026-07-13 对外商业化自查

- 目标按“本地安装、3–10名纯小白测试客户”评估。
- 功能矩阵53项：40项完整、11项部分、1项静态UI、1项未实现；功能清单覆盖约75%。
- 工作室内部试跑成熟度约60%；对外小范围商业化成熟度约40%。
- 当前最关键阻断：没有第一件真实可售商品闭环、安装环境未锁定、缺少启动器/更新/恢复/诊断、本机接口安全边界需收紧、120项历史商品测试被跳过、图片质量未用新商品对照验证。
- 当前完整工作区使用项目实际Python环境测试：332项，212项执行通过，120项历史运行商品测试跳过。
- 工作台一键启动入口已经补齐；旧`运行任务.command`仍写死开发机绝对路径，后续清理旧入口时一并移除或改造。
- 选品与关键词模块当前可演示但仍为未提交Beta：详情打开后滚动位置错误、部分横向截断、商品图缺失、更新时间未记录、单商品关键词可能为空。
- 详细截图和结论保存在`logs/commercial-audit-20260713/`。
- 在不增加新功能的前提下，完成出售前阻断项后，3–10名付费封闭测试预计还需3–5周；可自助安装和稳定日用预计6–10周。
- 用户已明确商业化不是当前重点；近期优先完成第一件真实可售商品闭环和工作室实际效率验证。

## 2026-07-13 来源可追溯的属性自动补全

- 已修复“只有逐字出现在1688原文里的属性才填写”的过度保守逻辑。现在会读取标题、选中SKU、规格、图片分析和已保存商品资料，再补齐有明确依据或低风险结构判断的类目属性。
- `P000001`的43项类目字段目前已填写22项，3项必填全部完成；原来只有14项有值。新增实际值包括：搅拌机类型=手持式、包装清单、旋转杯体=否、结构特点=无线、杯体材质=金属、外壳材质=不锈钢、行星结构=否、标题模板型号名称。
- 包装清单按SKU事实生成：`Миксер-стакан — 1 шт. Крышка входит только в комплектацию варианта 400 мл.`，并保存来源证据、推断类型和置信度。
- 功率、档位数、保修期、认证、保护系统、商品标识码、海关编码和电源线长度等没有来源的高风险字段仍保持`unknown`，不会为了填满页面而虚构。
- 350 ml与400 ml是SKU差异；当前类目快照不允许把容量作为合并维度，因此不会写成一个错误的全商品共同值，继续按独立商品卡处理。
- 修正尺寸字段映射：`Размеры, мм`只允许使用商品尺寸，不再把包装尺寸误填为商品尺寸；包装尺寸仅进入明确的包装字段。
- 真实工作台已验证上述字段可见、可编辑，并显示`1688资料`或`AI估算+置信度`，不是静态UI。

## 2026-07-13 图片旧缓存与问题图处理

- 工作台图片地址现在使用文件修改时间和大小生成版本号，并返回禁止浏览器缓存的响应头，重新生成后不会继续显示旧图片。
- 图片文件存在但尚未通过硬检查时显示`质检中`，不再误显示`已完成`。
- `detail-004`、`detail-007`、`detail-008`已清除旧成品并加入单图重试；其余已通过图片保留，不重复生图。
- 当前商品仍为安全停止状态：`STOPPED / 78% / next_action=image_generation`。下一次继续只处理上述3张问题图，不重跑标题、标签、SKU、价格和属性。
- Ozon商品写接口0次；库存接口0次。

## 本阶段验证结果

- 完整自动测试：348项，结果`OK`；233项实际执行通过，115项历史运行数据测试跳过。
- 新增覆盖：来源可追溯的可选属性推断、包装清单、风险字段保持未知、商品尺寸与包装尺寸隔离、图片版本缓存、未质检图片状态。
- 真实工作台浏览器验证：7项新增属性均显示正确值；2张主图和5张详情图显示`PASS`，3张问题图显示`RETRYING`。
- 本阶段主要修改：`ozon-field-completion/ozon_field_completion/service.py`、`scripts/run_batch.py`、`collector/local-ingest/app.py`、`collector/local-ingest/static/workbench.js`、`config/ozon-attribute-translations-zh.json`、`tests/test_source_backed_attribute_inference.py`、`tests/test_workbench_gap_fill.py`。

## 2026-07-14 属性标准化与图片超时修复

- Ozon 字典属性自动清理来源说明、中文备注和置信度文本，并按中文/俄文近义词匹配标准值；非必填匹配不到时不填。
- `P000002` 的材质属性 `6383` 已转换为标准值 `Нержавеющая сталь`，上传前检查已通过，Ozon 写接口0次、库存接口0次。
- 修复标题构建函数缺少返回值导致商品资料阶段失败的问题。
- 风格选择在已有商品资料时自动选择最高匹配风格，不再因风格分数接近而阻塞无人值守批次。
- 图片阶段加入超时后保留已完成图片、跳过未完成图片并继续的处理逻辑，后续按单图断点补图。

## 2026-07-16 联网大模型不可用处理规则（当前权威）

- 用户已明确：联网大模型暂时不可用时，任务停在当前步骤并自动重试；恢复后从原断点继续。
- 禁止使用本地规则、本地脚本或简化模型代替商品分析，因为替代结果不够准确，不能继续流入俄文文案、属性和生图。
- 服务等待不计为商品失败，不增加商品步骤失败次数，不删除已完成文件，不推进`completed_steps`，也不触发Ozon或库存接口。
- 工作台会明确显示“等待联网大模型恢复”，默认约30秒后自动重试，无需人工点击。

## 2026-07-14 P000004 分析通道故障恢复（历史记录，兜底方案已于2026-07-16作废）

- 失败根因已确认：不是商品资料或Ozon接口，而是Codex子进程访问分析服务时连续收到`403 Forbidden`，因此没有生成`output/product-analysis.json`。
- 当时曾加入本地事实兜底；该方案已按用户最新确认删除，不再作为任何商品的恢复路径。
- `P000004` 已生成并通过 `templates/product-analysis.schema.json` 校验，包含3个真实SKU、标题卖点、场景、尺寸/重量人工确认来源和明确unknown字段；未调用任何Ozon写接口或库存接口。
- `P000004` 已从失败断点恢复并自动完成`category_match`与`variant_rules`，当前下一步为`measurements`；不会重复CREATE。
- 当前恢复方式：服务性403、WebSocket/传输断开或Codex执行器不可用时只等待联网大模型恢复并自动重试；普通商品数据错误仍按原规则停在对应步骤并显示具体原因。

## 2026-07-14 商品生产测试问题修复收尾（当前权威记录）

### 已直接修复

- SKU差异不再按固定“规格1”猜测：采集到的`sku_image_prop_name/value`会参与判断；颜色类SKU按真实SKU逐个映射俄文颜色和Ozon字典值，不再把所有SKU都写成红色。
- 图片规划读取最终商品资料和每个SKU事实；独立商品卡不再继承多颜色标题、错误颜色、错误配件或其他商品的图片提示词。每个SKU使用自己的主图，共享详情图只复用，不重复生图。
- “带盖/带门”等结构特征不再被误写进包装清单；包装清单只读取明确的独立配件事实，未知就保留`unknown`。
- 人工确认的尺寸和重量保留`estimated_human_approved`证据；对外Ozon属性使用合法的`AI_estimated`来源值，避免Schema拒绝，同时不把人工确认降成无依据的普通估算。
- 采集器过滤平台价格说明和无关长文本，并从结构化商品数据补充来源属性；原始采集事实仍写入`input/source.json`，不覆盖原图。
- 类目/属性预检在图片前不再强制要求不存在的`ozon-draft.json`；只按已锁定类目、规则快照和必填属性检查。非必填字典匹配不到直接不填。
- 删除库存误报：预检不会再因为没有`stock`或`warehouse_id`而失败；上传请求仍不包含库存字段，库存接口调用保持0。
- 上传前只读查重和运行环境统一；`offer_exists_check`正确继承生产模式。批次线程发生单商品异常时只记录该商品失败，其他商品继续，不再整批被一个异常拖停。
- 旧批次状态恢复更稳健：失败步骤允许从断点继续；已经有后续成功上传记录的商品不会因为早期重试失败日志再次被工作台隐藏。`P000002`现通过状态结构校验。

### 验证结果

- `scripts/project_self_check.py`：`PASS`，15项检查，0失败、0警告。
- 核心回归测试42项通过、3项条件跳过（变体、采集器、容量变体、图片流程、批次确认）；旧商品夹具缺失的测试被明确识别，未恢复已按要求删除的商品数据。
- 全量测试本次发现359项，其中15项旧断言失败、38项因已删除旧商品夹具报错、22项跳过；问题集中在`P000005/P000009`缺失及旧价格/图片数量/风格预期。它们不是当前商品运行代码的新增失败；后续应改成自带临时夹具。
- 本轮未调用任何Ozon接口：CREATE 0次、UPDATE 0次、只读查询0次、库存接口0次。

### 本轮修改范围

`variant-compatibility-checker/variant_compatibility_checker/service.py`、`ozon-field-completion/ozon_field_completion/service.py`、`scripts/marketplace_content_generator.py`、`scripts/image_planner.py`、`scripts/run_batch.py`、`scripts/validate_product.py`、`ozon-adapter/ozon_adapter/service.py`、`scripts/ozon_metadata_matcher.py`、`collector/edge-extension/content.js`、`collector/edge-extension/src/content.ts`及本文件和`PROJECT_HANDOFF.md`。

### 下一步

1. 用下一件真实商品从1688采集开始跑一次，确认资料先完成、最后生图、失败只跳过单图/单商品。
2. 生图失败时保留已完成图片并单图重试；没有成功图片不得进入Ozon上传，但不会停止其他商品。
3. 在用户主动点击上传前继续只读查重；pending或状态不明确时禁止重传。
4. 逐步把依赖已删除旧商品的测试改成临时夹具；不恢复真实商品资料，也不触碰选品关键词模块。

## 2026-07-15 错误提示与修改入口

- 商品卡上的失败信息已改为白话：说明发生了什么、停在哪一步、下一步怎么处理；同时提供“立即修改”按钮。
- 预览检查页的失败提示已改为白话，保留“查看技术详情”用于排查；新增“立即修改”和“从失败步骤继续”两个明确动作。
- 点击“立即修改”会打开对应商品，并自动切换到资料、图片、价格、类目或店铺面板，定位并高亮第一个需要处理的字段。
- 风险面板和系统通知也使用同一套白话错误说明，技术原文只在展开详情时显示。
- 未调用Ozon接口：CREATE 0次、UPDATE 0次、只读查询0次、库存接口0次。
- 验证：`node --check collector/local-ingest/static/workbench.js`、`python3 -m py_compile collector/local-ingest/app.py`、工作台相关 unittest 62项通过、项目自检15项 PASS。

### 下一步

1. 用真实失败商品打开采集箱和预览检查页，确认“立即修改”能准确跳转并保存字段。
2. 继续把历史测试改为自带临时夹具，不恢复已删除商品数据。

### 2026-07-15 P000004 继续任务修复

- 根因已确认：P000004 是 `CONTENT_GENERATED / 78% / image_generation` 的已授权断点，`active_step=null`、没有活动 worker；前端却把所有中间状态误判为“运行中”，因此继续按钮被禁用。
- 现在只有真实 worker 或排队/上传状态才锁定按钮；已完成到断点的商品显示“继续生成”。
- 已授权断点直接从下一步恢复，不再重复一次批次确认；手动生成阶段不强制选择店铺，自动上传仍必须先选择已验证店铺。
- P000004 当前不会重复已完成的资料步骤，继续后从 `image_generation` 开始；Ozon CREATE/UPDATE/只读查询/库存接口仍为0。
- 采集箱状态文字也改为“待继续”，不再把没有活动进程的 78% 断点显示成“生成中”。
- 进一步去掉已授权断点的二次确认弹窗；点击“继续生成”即提交恢复请求，上传动作仍保留独立确认。
- 验证：前端语法、后端编译通过；`test_batch_confirmation`、`test_workbench_gap_fill`、`test_lan_collaboration` 共61项通过；工作台服务已重启并返回HTTP 200。

### P000004 断点按钮修复

- 修复`CONTENT_GENERATED`、`CATEGORY_MATCHED`、`IMAGES_GENERATED`、`PRICED`状态被误显示为“查看进度”的问题。
- 这些状态现在统一显示“继续生成”，会沿用`next_action`从断点继续，不重复已完成步骤。
- P000004当前仍为78%，下一步是`image_generation`；Ozon写接口仍为0次。

### 2026-07-15 自动连续流程修复（本轮）

- 根因补齐：旧修复只处理了按钮和断点显示，后端仍允许旧的手动确认设置生效；P000004因此停在`CONTENT_GENERATED / 78% / image_generation`且没有活动worker。
- 工作台新批次和单商品续跑现在强制`auto_upload=true`：点一次运行后按“文字→图片→硬错误快检→上传前检查→Ozon上传”连续执行，不再进入二次确认或`manual_ozon_upload`。
- 旧的本地手动模式配置会被自动归一为“自动连续流程”；店铺选择默认全部已验证店铺，仍可取消某些店铺；未选择店铺会在启动前明确提示。
- 单商品从失败断点或`CATEGORY_MATCHED`、`PRICED`、`CONTENT_GENERATED`、`IMAGES_GENERATED`续跑时，会保存快照并直接入队，不会重做已完成步骤。
- 单个图片或商品失败仍按原规则重试/隔离，其他商品继续；不改变库存禁止、pending禁止重传、重复CREATE防护。

验证：前端语法、后端编译、工作台/批次/局域网测试61项通过；项目自检15项PASS；工作台API返回自动连续流程；本轮Ozon CREATE/UPDATE/只读查询0次，库存接口0次。
## 2026-07-15 图片阶段实时进度与超时修复（本轮）

- 现象：P000004 图片子进程仍在运行时状态一直停在 78%，工作台看不到已完成图片；旧配置在多槽位时可能把单次图片阶段放大到 20 分钟。
- 修复：批处理器轮询图片子进程时按 `image-hard-gate.json` 和已落盘文件增量写入 `completed_image_slots`、`planned_image_slots` 与进度；图片阶段硬上限统一为 600 秒，超时保留已完成图片并只标记未完成槽位，后续步骤继续执行。
- 当前运行：P000004 批次 `B-3A8AE7EFF678` 已生成 6/11 张，`api_write_count=0`，仍在旧进程收尾；不重复生图、不调用库存或 Ozon 写接口。
- 验证：`tests.test_pipeline_optimization`、`tests.test_batch_confirmation`、`tests.test_workbench_gap_fill` 共 57 项通过。

## 2026-07-15 第7、8张图片恢复完成

- 根因：图片硬校验已通过，但旧的 `image-qc-report.json` 只记录了前6张，最终上传前检查读取旧报告后误报“图片不存在或未通过质检”。
- 修复：图片质检阶段每次进入都从最新 `image-hard-gate.json` 重建报告，不再复用过期报告。
- P000004：11/11张图片已生成并通过硬校验和图片质检，已到 `OZON_READY`，等待人工上传边界；本轮没有调用Ozon写接口，CREATE/UPDATE/只读查询均为0，库存接口0。
- 继续按“先文字、后图片、最后上传”验证下一件真实商品。

## 2026-07-15 故障演练与当前风险清单

### 已模拟并确认会拦截

- 缺少来源、主图、SKU图、最终类目或超过10个SKU：前置校验失败，不进入生图或上传。
- 类目规则快照与`category_id/type_id`不一致、必填属性缺失、字典值非法、`is_aspect`与SKU合并关系不一致：上传前阻断。
- 价格为空/非正数、包装重量或尺寸不大于商品本体、30个标签不满足：上传前阻断。
- 图片缺失、硬校验失败、主图/详情图数量不完整、商品特征被改写、图片质检报告过期：阻断；过期报告已修复为每次重建。
- pending或远端状态不明确、重复offer_id、店铺未配置：禁止再次CREATE/重传。
- 运行时锁文件、状态历史非法跳转、系统Python缺少依赖：会显示环境/状态错误；运行必须使用项目`.venv`。

### 会自动恢复或隔离，不应拖停整批

- 单张图片超时（单商品图片阶段上限10分钟）：保留已完成图片，只将缺失槽位标为待重试并继续其他商品。
- 单张图片生成失败：只重试该槽位，仍失败则该商品进入失败待处理，其他商品继续。
- AI分析传输失败：自动重试一次；仍失败时保留来源事实，不把未知内容伪造为确定值。
- 单店铺上传失败：只重试失败店铺，其他店铺独立保存状态。
- 安全停止：保存断点，后续从`next_action`继续，不重复已经完成步骤。

### 仍可能导致单商品停在失败

- 关键商品身份、SKU容量/颜色差异或类目无法从来源确认。
- 必填属性没有合法近义值，且平台拒绝未知值。
- 连续图片质检失败（空白框、白底、无卖点文字、错误SKU、改变结构）。
- 文案或字段生成连续两次失败，或Ozon返回业务错误。
- 本机服务停止、局域网地址/端口不可达、防火墙拦截、工作台与插件不在同一服务地址。

### 本轮离线验证

- `validate_product.py products/P000004`：通过。
- 图片质检报告：11/11张通过。
- 目标回归测试：70项通过。
- 全量测试（项目虚拟环境）：376项，其中23项失败、34项错误、22项跳过；主要是已删除商品夹具和历史断言，不能视为全绿，下一步改为临时夹具。

### 重要安全记录

故障演练期间发现旧工作台自动连续任务`B-D0A8CD0536E2`在17:47自动启动，并实际产生1次Ozon写入：店铺`zhonglian1`，`task_id=5095805941`，当前商品状态为`PENDING_REMOTE`。本轮之后没有再发第二次写入；当前只允许只读回查，禁止重传；库存接口调用仍为0。

> 注：以上是多店铺补写前的历史快照；下方“当前最新状态”已记录后续两家失败店铺的安全补写结果。
### 2026-07-15 多店铺同步上传修复（当前最新状态）

- 修复了“已有一家店写入后，其他明确失败店铺无法继续”的断点判断。现在显式指定失败店铺时，不会被全局历史写入次数拦截；仍会跳过`PENDING_REMOTE`、成功或状态不明确的店铺。
- 新增“并行重试全部失败店铺”入口：一次批次同时提交所有可安全重试的失败店铺，失败店铺之间互不阻塞，仍按店铺分别保存`task_id`、`payload_hash`和状态。
- P000004已完成本次安全补写：`zhonglian2`任务`5096278947`、`zhonglian5`任务`5096282804`，两家均已提交并进入`PENDING_REMOTE`；原先的`zhonglian1`任务`5095805941`未重复提交。
- P000004当前按店铺统计实际Ozon写接口共3次（每店1次），不是重复CREATE；库存接口调用仍为0。三家都只允许只读状态回查，禁止再次上传。
- 本次“其他店铺同步写入”已由同一条批量重试命令完成，不需要人工逐店点击。

后续动作：只读回查三个任务的远端状态，等待Ozon返回最终`product_id`和审核结果；若远端仍为`pending`或状态不明确，不再发送写请求。

只读回查结果（最新）：`zhonglian2`已被Ozon确认导入，三个商品ID为`5501114713`、`5501118278`、`5501061716`，状态为已审核/待库存，不是“没有上传”；`zhonglian1`仍为远端处理中，`zhonglian5`仍未返回商品条目。三店合计写接口仍为3次，回查本轮写接口0次、库存接口0次。

## 2026-07-15 空白图片占位框与断点修复（本轮）

- 新增图片质检的空白占位框检测：扫描成品图片上下信息区的低方差大面板，发现空框立即记录 `empty_placeholder_panel`，阻止上传，并只把该图片加入单图重做队列。
- 图片质检报告和工作台会显示白话提示：“图片里有空白占位框”；其他已经通过的图片保留，不会因为一张坏图重做整套。
- 旧版图片方案没有像素锁清单时，已落盘的PNG仍会计入完成槽位；新版有像素锁时继续要求锁清单，修复了图片已生成但进度停在78%的旧断点。
- 属性补全继续按锁定类目和真实字典值匹配；包装清单只接受明确的独立配件，不把“带盖/带门”等结构特征误写成配件。

### 本轮验证

- `scripts/project_self_check.py`：PASS，15项检查，0失败、0警告。
- 定向回归：72项通过，2项条件跳过；覆盖属性近义值、包装清单、图片断点、图片质检、批次确认、多店上传和工作台入口。
- 空白占位框检测单测：2项通过；49张现有本地PNG扫描未误报。
- 全量测试当前为379项：23失败、20错误、35跳过，剩余问题集中在已按要求删除的旧商品夹具和旧断言，不是本轮新增运行时错误；下一阶段改为自带临时夹具。
- 本轮没有调用任何Ozon写接口、只读接口或库存接口：CREATE 0、UPDATE 0、只读查询 0、库存 0。

### 下一步动作

1. 用一件新采集商品做真实端到端演练，确认先完成文字/属性，再生成带俄文卖点的主图和详情图。
2. 若出现空白框，只重做该槽位；若单商品最终仍失败，保留原因和“继续/重做”按钮，其他商品继续。
3. 把全量测试中引用已删除商品的测试改为临时夹具，不恢复运行商品数据。

### 2026-07-15 处理超过一小时的断点修复

- 查到当前没有运行中的 `run_batch` 或图片子进程；停留的是 `P000003` 的旧失败断点，不是真正在后台处理一小时。
- 旧规则把 SKU 原图 576px（829×576）按“低于600px”直接硬阻断，导致图片计划停在78%；现在 512–599px 仅显示清晰度警告并允许参考编辑，低于512px才阻断，仍禁止放大、抠图和像素复制。
- 已重新生成 `P000003/output/image-source-preflight.json` 与 `image-plan.json`：该SKU现在为 `ready_with_warning`，主图为 `planned/edit_real_image`。
- 已清除 `P000003/status.json` 的过期 `active_step`，恢复为 `CONTENT_GENERATED / next_action=image_generation`，不会再显示假运行；点击继续即可从图片步骤开始，不重复文字、属性或类目。

### 2026-07-15 断点状态一致性复核

- `P000003` 已通过 `validate_product.py`；旧失败原因仍保留，新的恢复记录已追加，图片步骤显示为待开始而不是失败或假运行。
- 当前没有运行中的批处理或图片子进程；项目自检 15/15 PASS，Ozon 写接口 0、只读查询 0、库存接口 0。

### 2026-07-15 P000004 异步上传回查

- `zhonglian1` 任务 `5095805941`：仍为 `processing`，3 个 SKU 均未分配 `product_id`，无错误回执。
- `zhonglian5` 任务 `5096282804`：仍为 `processing`，Ozon 返回条目数为 0，无错误回执。
- 已完成本轮只读回查并同步本地店铺记录；没有新增写接口，不允许重复 CREATE。当前更像 Ozon 店铺侧异步导入队列未出最终结果，而不是本地商品资料或图片校验失败。

### 2026-07-15 插件混合内容连接修复

- 修复1688 HTTPS页面直接请求局域网 HTTP 工作台导致的 `Mixed Content` 拦截。
- 插件内容脚本的商品提交、类目规则、收藏/最近类目请求改由扩展后台 service worker 转发；工作台地址仍限制为本机或局域网地址。
- 插件版本升级为 `0.4.8`，新增 `collector/edge-extension/background.js`；类目缓存和上传数据结构不变。
- 定向回归 21 项通过，JavaScript/Manifest 语法检查通过。项目自检唯一失败为既有 `P000005` 商品结构校验，不由本次改动引入；Ozon写接口0、库存接口0。

下一步：在 Edge 扩展页点击“重新加载”插件，再刷新 1688 商品页。

### 2026-07-15 改类目后无法继续修复（当前最新）

- 根因一：类目修改后旧的 `active_step` 会把已经重置为 `COLLECTED` 的商品误判为仍在运行，工作台因此禁用继续按钮；现在改类目会明确清空 `active_step`，前端只按真实 `QUEUED/PROCESSING/UPLOADING` 状态锁定按钮。
- 根因二：中文属性值（例如“不锈钢”）在转俄文字典前被清洗为空，类目属性匹配在 `max()` 处崩溃；现在先做中文/俄文同义词映射，并对空值返回 `unknown`，不会再把整件商品卡死。
- `P000005` 已用新类目重新完成只读类目匹配：`category_id=17027933`、`type_id=97498`、`match_status=api_confirmed`，已生成30项类目属性；后续可从“继续生成”进入下一步。
- 当前商品状态不代表已上传：P000005 `CATEGORY_MATCHED`、`api_write_count=0`、Ozon写接口0、库存接口0；没有重复CREATE。

### 本轮验证

- 类目/工作台定向回归：74项通过。
- `node --check collector/local-ingest/static/workbench.js`：通过。
- P000005 `category_match` 本地续跑：通过，新类目和30项属性写入成功。
- 项目自检：15项中唯一失败仍是 P000005 商品结构校验；这是类目修改后文字、图片和上传草稿已按规则失效，等待点击“继续生成”重新生成，不是本次修复新增的崩溃。
- 本轮 Ozon 写接口0、库存接口0；没有调用Ozon写接口。

### 下一步动作

1. 在工作台打开 P000005，点击“继续生成”；系统会从待处理的文字/属性步骤继续，不需要重新采集，也不会重复上传。
2. 若新一轮仍停住，界面应显示具体失败步骤和“立即修改/从失败步骤继续”，不再显示假运行锁定。

## 2026-07-16 P0 实施完成（历史记录，已由顶部旧任务归档规则取代）

> 以下是P0最初实施快照；随后用户确认旧商品全部归档并授权停止存活通道，当前真实状态以文档顶部权威节为准。

### 本轮已完成

- 已在 `runtime/p0-snapshots/20260716-001217/` 保存 Git、运行队列、商品状态、店铺发布记录和进程快照。
- 已建立 SQLite 任务库 `runtime/task-db.sqlite3`，完成 `001` 与 `002` 迁移；`runtime/task-db-cutover.json` 已标记 `sqlite_authoritative`。旧 `store-publications.json` 只读备份为 `.readonly-backup`，不再长期双写。
- 已建立商品总状态、店铺发布状态、店铺 SKU 发布明细、任务回查和事件表；一个商品可同时出现 CREATED、PENDING_REMOTE、FAILED，按所有目标店铺聚合。
- 当时曾启动独立 `scripts/remote_status_worker.py` 做只读回查；随后已按新永久规则停用并禁止自动启动，手工刷新只返回停用提示。
- P0 已禁用主工作台自动启动财务同步；财务独立进程和统一启动器拆分留到后续阶段，不能写成已经完全拆分。
- 图片上传硬门禁已收紧：上传入口每次都按当前 `image-plan.json` 和实际文件重建检查，不信任历史 PASS；每个已选 SKU 必须有主图，每个商品必须有8张详情图，缺图、损坏、尺寸、重复、空白框、水印或 SKU 绑定异常均禁止上传。
- 远端回查使用 `next_check_at` 退避：首次1分钟，之后5分钟、15分钟、30分钟，长时间Pending最多每小时一次；手工刷新仍可立即只读查询。
- `runtime/task-db.sqlite3` 及 SQLite sidecar 已统一确保权限为0600；新建、迁移和恢复都会重新校正权限。
- 已完成模拟压力测试：30 商品、20 类目、10 店铺、每商品3 SKU，状态聚合与店铺隔离通过。

### 当前真实状态

- `P000004`、`P000005`及其他旧商品随后已统一转为本地`ARCHIVED`；原有task_id、商品ID和摘要保留，未重传。
- 旧图片硬检查结论仍保留在恢复资料中，但这些任务不再进入工作台或上传流程。
- 历史异步回查记录仅作摘要保留；归档动作之后不再产生回查。Ozon写、UPDATE、只读回查、库存接口均为0。
- 归档前的图片任务已通过stop-file正常退出；当前真实图片进程和回查进程均为0。
- 已按PID清理13条确认失效的旧 `.pipeline.lock` / `image-status-monitor.pid` 记录；当前没有商品锁或失效PID文件，未删除或终止任何仍存活的图片进程。

### 验证结果

- Python 编译检查、工作台 JavaScript 语法检查通过。
- P0、店铺隔离、批次授权、采集箱和局域网定向回归共 35 项全部通过；新增“旧 PASS + 缺详情图上传前硬阻断”测试另行通过。
- 新增上传器硬门禁测试：1项通过；验证旧 `final-upload-check.json=PASS` 且详情图缺失时，`execute_upload` 在任何写请求前失败，记录的写接口调用为0。
- P0组合定向测试共37项通过；全量 `unittest discover -s tests -p 'test*.py'` 实际运行390项：通过321项、失败25项、错误19项、跳过25项。失败/错误集中在旧商品夹具、旧类目/价格/图片数量预期和插件发布包版本，不是本轮P0新增回查或写入逻辑失败。
- 当前 `P000004/P000005` 写接口计数仍为各3次（本轮未增加），库存接口计数为0；远端回查本轮日志均为0写入/0库存。
- 旧 `test_ozon_field_completion` 仍有历史运行夹具漂移（P000004 图片清单/属性期望与当前真实数据不一致），不是本轮 P0 状态库改动引起；需后续单独整理夹具。

### 下一步

1. 由用户明确授权后再安全停止存活的图片 worker；在此之前只做PID复核，不执行终止。
2. 更新旧字段完成测试夹具，补充 SQLite cutover 后的回滚演练。
3. P0 验收通过后再进入真实 30 商品试跑；真实 95% 成功率不在本轮模拟测试内。
