# PROJECT HANDOFF

更新时间：2026-07-13（Asia/Shanghai）

## 项目定位

`crossborder-ai-factory`是现有本地工作流项目，不是新项目。目标主流程：

`1688采集 -> 选择SKU -> 选择最终Ozon类目 -> 采集箱 -> 批次生成资料/定价/属性/图片 -> 手动检查或自动审核 -> 多店Ozon CREATE/UPDATE -> 异步状态回查`

当前阶段不做销售分析、上架后自动调价、广告、预算、转化/退货运营分析，也不接OpenAI API或其他第三方AI API。

## Git基线

- 项目根目录：`/Users/apple/Documents/crossborder-ai-factory`
- 本地分支：`main`
- 基线提交：`a8f8b58df5f3e06f7f924e45b348a555d3fc0375`
- 基线标签：`baseline-before-category-selection`
- 基线提交信息：`baseline: current crossborder-ai-factory before category workflow update`
- 基线包含327个文件，约166.15 MiB，主要体积来自必须保留的Ozon本地规则/类目缓存。
- 没有配置远程仓库，没有push，没有上传GitHub。

## 敏感信息处理

- `.env`、`.env.*`（模板除外）、真实店铺密钥、Token、Cookie、密码、本地访问码、数据库、运行队列、商品数据、图片缓存、日志和临时文件均已加入`.gitignore`。
- `ozon-adapter/.env.zhonglian1`含真实本地店铺凭证：已保留使用、未加入Git、未输出内容。
- `config/lan-access.json`含本地访问码：已保留使用、未加入Git。
- `ozon-adapter/shops.json`只含环境变量引用和店铺元数据，可纳入版本控制。
- 测试中的`client-secret`和`api-secret`为假数据，不是生产凭证。

## 已执行的数据清理

用户明确要求旧商品全部删除、不保留；店铺配置继续使用。因此已永久清理：

- `products/`内全部商品资料和图片，仅保留`.gitkeep`。
- `batches/`内全部批次，仅保留`.gitkeep`。
- `runtime/`内全部运行状态，仅保留`.gitkeep`。
- 运行日志、PID、锁、队列、图片缓存、缩略图缓存、临时公网通道状态和旧发布ZIP。

保留：

- Ozon店铺配置和本地密钥。
- 局域网访问配置。
- Ozon中文类目树、属性规则、字典值、`is_aspect`和规则缓存。
- 全部源码、测试、Schema、迁移、规则模板、Skills、README和交接文档。

## 本阶段功能完成

### 任务式工作台

- 主入口：`我的采集箱 / 需要我处理 / 已上架商品`。
- 店铺设置和系统设置只对负责人显示。
- 每个商品卡只有一个随状态变化的主要动作，减少寻找功能的时间。
- “需要我处理”只集中显示关键问题、明确失败和已生成待上传商品。
- 电脑端1280×720和手机端390×844真实浏览器验收通过。

### 成员商品隔离

- 新商品采集时保存`input/owner.json`。
- 成员访问码以SHA-256哈希保存；访问码只在创建/重置时显示一次。
- 每个成员只能看、改、运行、停止、删除、导出和上传自己采集的商品。
- 批次使用`batches/<batch_id>/owner.json`绑定所属成员。
- 通知、商品列表、详情、图片、店铺选择、发布记录、日志、风险和导出均由后端按成员过滤。
- 负责人可以管理成员、店铺和全局设置，但没有跨成员查看商品的权限。
- 成员可以选择全部已配置店铺，但不能修改店铺凭证和全局设置。
- 删除仍有商品的成员会被阻止，避免商品成为无主数据。

### 通知和关键提问

- 工作台每15秒读取当前成员的通知。
- 支持浏览器/系统桌面通知；点击通知会聚焦工作台并打开对应商品。
- 只有商品身份、SKU/变体/颜色映射、结构、配件数量、真实参考图等关键事实冲突或缺失时才创建问题。
- 普通可选属性未知不会打断任务。
- 回答保存到`input/operator-guidance.json`，只用于对应问题并可追溯。

### 图片视觉与速度

- 支持整组简单风格意见和单图意见。
- 意见进入风格选择、图片规划和生图提示，但不能覆盖真实性硬规则。
- 类目、原始图、视觉意见和关键输入参与哈希；未变化的步骤复用缓存。
- 风格变化只从风格选择阶段向后失效，不重复分析无关步骤。
- 仍保持每SKU真实主图、6–8张共享详情图、非白底、逐图失败重试和已完成图保留。

### Edge采集插件

- 版本升级到`0.4.6`。
- 页面使用“成员访问码（决定商品归属）”说明。
- 继续要求SKU不超过10个和最终Ozon类目必选。
- 中文类目树和本地缓存继续使用，不在每次采集时重新加载Ozon。

## 当前数据与接口状态

- 商品0、批次0、运行队列0。
- 店铺`zhonglian1`配置保留，凭证不在页面和Git中暴露。
- 本阶段Ozon CREATE 0次、UPDATE 0次、其他商品写接口0次。
- 库存接口调用0次。
- 未生成远端task_id/product_id，也没有pending任务。

## 最近验证

- 本阶段提交范围自动测试：318项，`OK`。
- 实际执行：198项通过。
- 跳过：120项，均为依赖已删除旧商品运行目录的历史集成测试。
- 当前整个工作区自动发现327项并全部`OK`；额外9项来自用户另行创建、未纳入本阶段提交的市场情报测试。
- 新增成员权限、数据隔离、关键问题、视觉意见和工作台测试全部通过。
- Python语法检查通过。
- 真实工作台API：服务健康、负责人会话、空商品、空通知、保留店铺均返回正确。
- 真实浏览器：三个主入口、系统设置、成员区、店铺状态、手机菜单均正常；控制台0错误/警告。

## 尚未完成与商业化判断

- 清理后尚未完成第一件真实可售商品的全链路上传和审核结果。
- 真实多店写入、逐店只重试失败店和异步回查需要用新商品验收。
- 图片质量需要用新商品与用户手动流程做真实对照，不能只靠自动QC判定。
- 120项历史集成测试需逐步改造成自带合成夹具。
- 商业化前建议完成30个真实商品、至少5个类目、连续7天无数据丢失；重复CREATE、库存接口和成员越权必须保持0。
- 当前更适合作为工作室内部试跑版，尚不适合直接售卖给外部用户。

## 下一步动作

1. 用最新版Edge插件采集一件新真实商品，确认SKU与最终Ozon类目。
2. 验证10分钟内生成资料、属性、价格和图片。
3. 以用户手动流程图片为质量标杆，使用简单整组/单图意见完成一次纠偏。
4. 本地预检和只读存在检查都明确后，再由用户点击运行真实上传。
5. 保存逐店逐SKU远端记录并异步回查到可售或明确失败。
6. 第一件成功后开始30商品工作室试跑，并同步清理历史测试夹具依赖。

## 永久业务限制

- 不提交库存字段，不设置`stock=0`，不调用库存接口。
- pending、上传中、审核中或状态不明确时禁止重传。
- 不重复CREATE；明确存在的商品只能安全UPDATE。
- 单商品最多10个SKU。
- 单商品或单店失败不阻塞整个批次；只重试明确失败店铺。
- 每店独立保存`offer_id/task_id/product_id/payload_hash`和状态。
- 类目由采集阶段用户最终选择，运行中不得替换。
- 属性和变体逻辑必须使用当前类目规则快照、字典值和`is_aspect`。
- 不虚构材质、承重、认证、功能和配件；估算值必须有标记、置信度和来源。

## 工作区注意事项

以下为用户另行创建的选品/市场情报内容，本阶段未修改、未删除、未纳入本阶段提交：

- `docs/Ozon选品与关键词模块方案.md`
- `market-intelligence/`
- `scripts/probe_ozon_market_sources.py`
- `templates/market-source-status.schema.json`
- `tests/test_market_intelligence.py`
