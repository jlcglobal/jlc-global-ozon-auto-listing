# PROJECT HANDOFF

更新时间：2026-07-17（Asia/Shanghai）

## 2026-07-17 整体目标与阶段结论

- 用户确认AI Factory现阶段优先服务自己的工作室，先稳定跑通真实商品生产，不以立即对外销售软件或扩张为通用ERP为优先。
- 当前目标闭环是：人工选择SKU、最终Ozon类目和目标店铺；系统完成俄文资料、属性、定价和N+8图片；人工最终确认；随后多店同时提交。每店返回有效task_id即本地交接完成，商品锁定只读，不自动回查、不提交库存，后续在Ozon商品卡后台处理。
- 下一验收先跑1件当前插件全新采集的正式商品；成功后再跑10件商品批量。P900002只证明图片标准，P000006只证明旧流程三店task_id交接，二者都不能单独替代新合同的正式端到端验收。
- 当前53项工作台需求为40项完整、11项部分、1项静态UI、1项已取消局部叠字。P0关键里程碑已完成4/5，最后一项是新商品按当前合同完成三店同时提交；这不是商业成熟度百分比，也不能表述为已经完全稳定。
- 当前运行证据：工作台健康接口`status=ok`；SQLite中P000006三家店铺均为`HANDED_OFF_TO_OZON`，各自write attempt=1、read query=0；旧status.json只作审计。图片合同检查点为`35afa53`，加入店铺专属offer_id后的完整回归484项通过、112项条件跳过。

## 2026-07-17 店铺专属offer_id交接

- 新的正式上传在网络请求前，为每个`SKU × 店铺`分配一个16位无规律英文数字货号。映射先保存到SQLite店铺发布记录，再写入该店隔离工作区；失败重试保持不变，已有task_id/product_id的历史记录绝不改号。
- 共享主档不改号；隔离工作区会同步替换`ozon-draft.json`和`variant-grouping-result.json`，并保存`store-offer-id-map.json`审计文件。每家店的首次提交被强制为CREATE，远端碰撞会在写请求前阻断，不能误UPDATE其他商品。
- 旧的产品级共享货号提前查询已经改为延迟到最终上传时按店铺检查，避免旧`Pxxxxxx-SKU`结果误导真实动作。本规则不能单独规避Ozon复制商品卡识别，商品内容、图片、属性、品牌和条码仍须遵守平台规则。
- 验证结果：多店多SKU唯一性、重试稳定性、SQLite切换后持久化、历史task_id不可修改、隔离草稿替换和CREATE门禁均有自动测试；完整484项通过、112项条件跳过，真实Ozon写入0、库存调用0。

## 2026-07-17 图片设计执行合同

- 图片策划已改为整套一次完成：联网`ozon-ecommerce-designer`在一次执行里输出当前商品全部N张SKU主图与8张共享详情图的文案、设计理由、艺术指导、模型原生排版坐标和提示词，随后才允许进入生图。
- 强制顺序为商品证据、买家分析、卖点排序、图片序列、逐图艺术指导、提示词完成、生成前校验；任一步缺失或乱序都只重试整个`ecommerce_design`步骤，不重跑上游采集、类目、测量和定价。
- 正式生产不再允许固定版式渲染器或后置叠字脚本兜底。每个图位只允许一次内置生图调用，在同一次调用中完成场景、商品和完整准确俄文；`designer_directed_overlay.py`正式CLI已禁用，仅保留旧审计测试函数。
- 俄文缺失、乱码、拼错或不可读时只重试失败图位一次。商品验收以类型、颜色、可见结构、配件和可信整体比例为准，不要求像素级复刻。
- P900002旧固定模板和不合格尝试均在拒绝区；11图计划已按单次成图契约重新物化。当前3张SKU主图+8张共享详情图已由用户整组验收通过，全部为1086×1448的3:4且文件哈希唯一。SKU对比图首版2:3已进入拒绝区并仅重试该图；三次网络错误没有导致整套重生。本次验收确认新的单次成图视觉标准可用于后续正式商品，但P900002仍是隔离测试身份，禁止上传。Ozon和库存调用均为0。本合同通过完整480项回归（112项条件跳过）和4个项目Skill校验。

## 2026-07-17 当前权威交接：task_id即完成交接，已提交商品本地只读

- 当前代码检查点为`35afa53 feat: validate single-pass Ozon image workflow`；旧`98411ca`仍保留为之前的生产工作台检查点。
- SQLite schema `003`把所有已有task_id的旧`PENDING_REMOTE`记录一次性迁移为`HANDED_OFF_TO_OZON`。P000006三家店铺现在统一为本地交接终态，工作台显示100%，不再自动回查，也不允许重复提交。
- P000006的历史`status.json`、旧图片目录契约和实际SQLite提交记录曾互相矛盾；现在SQLite task/store状态是提交事实源，旧JSON只保留审计。未提交的旧契约商品仍会被新版生产合同硬阻断。
- 终态商品只允许查看、浏览图片和复制URL。本地草稿、标签、属性、价格、店铺、建议、图片和类目相关页面控件均已禁用；对应后台写接口同时返回409，避免旧页面或直接请求绕过只读边界。
- 上传器在任何图片门禁或Ozon读取之前先检查本地幂等记录：一旦存在已完成写入的task_id，第二次写入直接阻断。默认测试同时覆盖无库存字段、库存端点白名单拒绝、已有offer UPDATE和内容未变SKIP。
- 当前完整默认回归：484项，`OK (skipped=112)`，0失败、0错误。关键类目/字段/上传安全测试不再整类依赖旧P000004夹具；真正依赖归档运行数据的历史验收仍明确跳过。475/480项结果仅为此前阶段基线。
- 当前工作台正式商品总数为1，仅P000006；P000007尚不存在。下一次真实验收必须来自当前1688插件新采集，不能从对话附件、P000006或P9测试数据复制。
- 本轮未调用Ozon写接口、只读接口、状态回查或库存接口，也未启动任何生产worker。

## 当前权威交接：商品/采集批次/测试素材/图片资产完全隔离

### 已落地的硬门禁

- 正式商品只能读取 AI Factory 工作台本次采集写入的 `products/<product_id>/input`，并且 `source.json`、`source-manifest.json`、目录和状态必须共同匹配当前 `product_id + collection_id`。每条来源记录保存 `source_kind=workbench_collection`、路径、SHA-256和采集时间。
- 创建批次、单商品运行、商品分析、设计和图片规划都会拒绝 `manual_test`、`test-data`、其他商品、旧归档商品以及任何生成输出。禁止按文件名、图像相似度、SKU名或相近规格跨商品补图、补规格或复用属性。
- 对话手工素材已经物理迁入 `test-data/manual-input/P900002`，结果只在 `test-data/manual-output/P900002`。项目中不存在正式 `products/P900002`；这个测试样板不能进入采集箱、payload、发布记录或Ozon队列。
- 正式图片目录以物理位置为准：原始素材、生成候选、已拒绝、已确认四类完全分开。工作台接口分别返回四个清单；图片规划只接受当前商品 `input` 下且已登记哈希的真实图片。手动模式 `accepted-images` 不完整时，历史PASS文件也不能绕过上传阻断。
- 新增项目级 `ozon-ecommerce-designer`，统一输出商品理解、俄文SEO资料、30个唯一标签、可追溯卖点、属性建议、SKU差异和整套图片销售故事。图片契约为 N个已选SKU = N张独立主图（N=1-10）+ 正好8张共享详情图，而不是固定3张主图。
- 运行完成进入 `WAITING_MANUAL_REVIEW / 等待人工检查`，不自动打开预览。手动模式不自动上传；自动模式仍受原有明确开关和全部安全门禁约束。

### 原始参考和失败样例纠正

- `references/manual-ozon-flow-2026-07-12/`中已经保存总提示词和8张手工流程截图，原临时文件未移动、未删除。它们只标记为版式/信息结构质量基准，不能当作当前商品的产品图。
- P900001不是用户的4 SKU圆形食品盒，且曾使用硬编码俄文和离线占位渲染，因此验收无效；现只在`test-data/offline-audit/P900001`保留失败审计，不得再宣称“真实链路已证明”。
- P900002是开发用冰箱收纳盒金标测试，只有3张手工SKU图；所有后续生成图都是测试输出或被拒绝输出，禁止自动回流为输入或未来商品参考。

### 当前验证和未完成项

- P900001 已从正式 `products/` 迁到 `test-data/offline-audit/P900001`。正式编号分配器只扫描P000001-P899999，所有P9测试身份都会被正式生产门禁拒绝。
- 批次创建已改为先全量校验、再冻结模式和来源快照；单商品与多商品共用同一函数。手动商品`WAITING_MANUAL_REVIEW`聚合为批次`AWAITING_MANUAL_UPLOAD`，不再误计为完成或上架成功。
- 人工确认图片新增不可篡改manifest；上传前逐张核对哈希、槽位、collection和设计版本。来源manifest的哈希也冻结进批次，整份来源与清单一起重写仍会被后续阶段阻断。
- MarketStore与关键SQLite调用已改为真正关闭连接；该阶段曾在`ResourceWarning`作为错误的条件下运行462项且无连接泄漏。当前主线已扩展到480项，`OK (skipped=112)`，0失败、0错误。
- `scripts/project_self_check.py`已区分当前契约、归档审计和旧目录契约商品。当前自检PASS（0错误、1警告）：归档P000001-P000005不再污染活跃校验；P000006作为旧契约数据透明提示且禁止加入新批次。
- 本地浏览器实测确认P900001不在工作台，四类素材区独立渲染，无浏览器控制台错误；新商品的接受/拒绝/替换/重生交互仍待全新采集输入。另修复了通知接口与SQLite有效状态不一致造成的虚假“需要处理”角标。
- P900002已执行真实整套生图并完成用户肉眼电商质量验收，证明图片执行标准；但它是隔离测试身份，不能代替正式商品。联网Codex/真实生图能力不可用时仍必须停在等待状态，不能用PIL模板冒充成品。
- 真实端到端验收仍缺一件由当前工作台新采集、由用户选择SKU/最终类目/三个店铺的正式输入。禁止用P000006、P900001、P900002或对话附件冒充；收到新采集后先完成本地生产和人工确认，再按用户授权同时提交三店，以各店有效task_id结束本地交接。
- 本轮真实调用计数：Ozon写接口0、Ozon只读/状态回查0、库存接口0；没有启动正式商品、批次或图片worker。

## 2026-07-16 俄文资料与电商图片专项重构

### 已完成

- 建立`ecommerce-creative-brief`作为俄文资料和图片之间的唯一交接层，避免商品分析、文案、图片各自重新猜测商品。
- 每个已选SKU固定一张独立主图；每个商品固定8张共享详情图。8个详情槽位只固定商业目的，视觉风格、场景、配色、构图和文案由商品本身决定。
- 结构、尺寸、SKU/包装内容等确定性证据图必须使用真实原图合成；场景和氛围图才允许使用真实商品参考图进行AI场景编辑。
- 俄文提示已明确要求SEO语序、真实商品理解、合法类目属性和可追溯卖点，禁止中文直译和虚构搜索热度。

### 当前验收边界

- 本轮不启动真实商品任务，不触碰Ozon写接口、只读回查或库存接口；P000001–P000006旧任务不重传。
- 更正：2026-07-12的原始提示词和8张截图已经找到并复制到项目reference目录，只作为版式质量基准。
- 下一步是生成独立离线样品的完整资料、Brief、逐图计划和8张技术可验收图片，再运行全量回归测试。
- 整改：原`products/P900001`不是同一手工基准商品且使用了硬编码/占位渲染，不能作为成功验收；已迁到`test-data/offline-audit/P900001`，不再作为真实链路证据。
- 全量回归实际结果：421项，355通过、22失败、19错误、25跳过。失败/错误均落在已归档旧商品夹具、旧价格/类目断言或缺失历史文件；新增专项测试和图片工作流回归通过。

## 2026-07-16 当前内容校验规则：禁止中文证据进入Ozon商品卡

- P000006三家店铺均收到相同Ozon提示，根因已由实际提交payload确认，不是Ozon随机误报：属性4384`Комплектация`混入中文取证句和本地图片路径；属性6378`Объем, л`收到带单位且取自错误“斤数”的文本。
- 当前代码已同时修复字段生成、SKU容量解析和上传前校验。以后内部证据可以保留中文，但提交给Ozon的标题和属性不能含中文/控制字符/本地路径，数值型属性必须是纯数字。
- P000006本地正确值：`Комплектация=внутренняя крышка, внешняя винтовая крышка`；四个SKU的`Объем, л`依次为`10/25/5/2.5`。完整人工修改清单见`products/P000006/output/ozon-card-correction.json`。
- 已提交的三店任务未被重传或UPDATE；历史payload不改写。后续人工在Ozon商品卡后台修改，AI Factory只负责保证新任务不会再次带着同类错误提交。
- 验证：隔离dry-run无内容阻断，生成的API模板值正确；定向测试通过，Ozon写接口0、远端回查0、库存接口0。

## 2026-07-16 当前权威协作方式：免访问码、全工作室共享

- 同一局域网内的电脑直接打开主电脑工作台，不再输入成员访问码。
- 所有电脑看到同一套商品、任务、预览、错误、日志和进度；旧成员归属字段只用于审计，不再隐藏商品。
- 设备按局域网电脑自动识别，所有本地写操作记录设备元数据；不会把请求正文或店铺凭证写入设备日志。
- 主电脑是唯一设置管理端：店铺密钥、连接测试、系统模式和安全退出仍只允许主电脑。远程电脑不因免码获得设置权限。
- 后端仍限制私有局域网，并验证同源工作台/采集插件来源；外部网页跨站访问会返回`UNTRUSTED_ORIGIN`。
- 工作台页面已删除访问码、添加成员和重置访问码入口；Edge插件`0.4.11`只保留主电脑地址并自动携带设备标识。
- 当前商品与历史数据不迁移、不重建、不重新上传。Ozon写入、远端回查和库存调用均为0。

### 其他电脑现在怎么用

1. 用浏览器打开主电脑局域网地址，例如`http://主电脑IP:8765/workbench`，刷新后直接进入共享商品列表。
2. 将Edge插件更新为`release/1688商品采集插件-0.4.11.zip`，在“主电脑连接”里只填写工作台地址并测试连接。
3. 采集、预览和运行可在任意工作室电脑操作；店铺与系统设置回主电脑处理。

本文后面关于“每人只看自己的商品”和“成员访问码”的段落均为历史实现记录，不再代表当前规则。

## 2026-07-16 P000006 SKU缺图P0修复（当前交接重点）

### 已完成

- 已确认P000006的实际问题不是生图逻辑：1688页面只渲染21/25个SKU图片，旧插件没有等待剩余SKU懒加载，却允许选择无图SKU。
- Edge插件版本升级为0.4.9。插件在预览/采集前自动滚动SKU虚拟列表、触发懒加载、重新读取图片；未加载完整时显示缺图数量并阻断采集，缺图SKU不能选择。
- 后端对新插件发送的全量预检和已选SKU再次硬检查，统一返回易懂错误`SKU_IMAGES_INCOMPLETE`；历史`final-upload-check.json`不能绕过当前图片检查。
- 新包已生成：`/Users/apple/Documents/crossborder-ai-factory/release/1688商品采集插件-0.4.9.zip`。
- P000006没有重新上传，Ozon CREATE/UPDATE/只读回查/库存调用均为0。

### 其他电脑更新方法

在Edge打开扩展管理页，移除或停用旧的0.4.8，解压并加载0.4.9包中的`edge-extension`目录，或按现有安装方式替换后点击“重新加载”。更新后先在1688页面确认插件显示“SKU图片已完整加载”，再点击采集。

### 验证

- 32项P0定向测试全部通过；JavaScript语法、后端编译和发布包版本校验通过。
- 选择缺图SKU、全量SKU缺图、旧PASS文件三种情况均在任何上传/创建前阻断。

## 2026-07-16 当前启动方式：桌面App、生图看门狗与断点恢复

### 可直接使用

- 桌面App：`/Users/apple/Desktop/AI Factory.app`。双击后复用或启动本地`0.0.0.0:8765`服务，并打开工作台；浏览器关闭不影响后台任务。
- 项目内App源：`mac-launcher/AI Factory.app`；后台守护：`scripts/run_workbench_service.sh`；原`启动工作室工作台.command`仍可使用。
- 工作台顶部显示真实主机状态，并提供`安全退出`。安全退出会写入停止请求，等待当前步骤落盘后停止服务；不是直接杀死任务。

### 生图恢复规则

- `image_generation_stall_seconds=300`，判定依据为5分钟内既没有新增完成图片，也没有进度心跳。
- 单商品图片并发3，商品级并发1；第一次卡死自动终止该商品生图进程组，保留已完成图片，只补缺图，自动恢复上限1次。
- 恢复中的商品让出队列，其他商品可继续。第二次失败后进入`needs_attention`，记录原因、显示一键继续并发出Mac通知，不无限重启。
- 服务重启时只恢复有既有运行授权的未完成新商品；归档商品、未确认手动任务和Ozon上传不会被自动启动。

### 模式与安全边界

- 默认`手动检查`：运行前确认，生成完成后保留最终人工上传门；负责人可明确开启自动模式。
- 旧任务仍全部归档且远端回查停用；本轮Ozon CREATE/UPDATE/只读/库存调用均为0。
- 定向测试79项全部通过。完整测试398项：25失败、19错误、25跳过；失败/错误/跳过数量与修改前基线相同，新增看门狗、恢复、启动器和手动模式测试均通过。

### 下一步验证

采集1件全新商品，真实走到最终上传确认页但先不上传，重点验证：类目/SKU/店铺确认、售价公式结果、3图并发、缺图重试、主机状态和中断续跑。之后再进入多店同时提交与属性人工修正的业务验证。

## 2026-07-16 当前权威交接：旧任务归档，Ozon回查永久停用

### 已完成

- 已安全停止旧`remote_status_worker`（PID 14136）以及P000004/P000005图片通道：13003/13004、89591/89592；全部正常退出，未使用`kill -9`。
- SQLite已升级为schema `002`，新增`archive_records`和`batches`本地状态表；数据库权限保持`0600`。
- P000001–P000005共5个旧商品标记为`ARCHIVED`；8条历史Ozon任务标记为`ABANDONED`并清空`next_check_at`。历史task_id、offer_id、product_id、payload_hash、写入次数和回查次数保留在不可变归档摘要中。
- 历史批次共25个标记为`ARCHIVED`。完整本地资料仍留在原目录，恢复清理清单位于`runtime/recovery-archive/`，统一保留7天，`cleanup_after=2026-07-23`。
- 工作台启动、首页、批次结束流程不再启动远端回查；刷新按钮改为“远端回查已停用”，只返回本地提示。
- 新提交语义已落地：写入返回有效task_id后本地状态为`HANDED_OFF_TO_OZON`，不会再等待商品ID，也不会自动调用远端回查。
- 临时图片通道固定24小时TTL，提交后保持独立存活，不依赖远端回查关闭。

### 当前限制

- 本地只记录提交交接，不提供上架后自动运营、状态管理或UPDATE编排；商品卡后续动作由操作者在Ozon后台完成。
- 旧商品和旧批次在7天恢复期内不可物理清理；到期清理动作另行授权后执行。
- 本轮没有启动新商品，没有执行真实30商品试跑；Ozon写、只读、库存调用均为0。
- P0定向回归23项全部通过；全量测试394项：325通过、25失败、19错误、25跳过。失败/错误来自归档后仍指向旧商品目录的历史夹具、旧断言和插件版本预期，需后续单独迁移夹具；本轮新增归档、停回查、task_id交接和图片TTL测试均通过。
- Python编译、状态Schema、SQLite完整性/0600权限和工作台健康检查均通过。

### 验证记录

- 定向边界测试覆盖：批量归档与不可变摘要、回查停用零调用、task_id本地交接终态、图片TTL独立存活。
- 已完成Python语法检查、SQLite状态与权限检查、进程核验；完整测试结果将在本轮最终测试后更新。

## 项目定位

`crossborder-ai-factory`是现有本地工作流项目，不是新项目。目标主流程：

`1688采集 -> 选择SKU -> 选择最终Ozon类目 -> 采集箱 -> 批次生成资料/定价/属性/图片 -> 多店Ozon CREATE/UPDATE -> 返回task_id并本地交接完成`

当前阶段不做销售分析、上架后自动调价、广告、预算、转化/退货运营分析，也不接OpenAI API或其他第三方AI API。

## 2026-07-15 最新交接：财务同步熔断与退避

- 真实故障：23:00后产生77条计划同步失败；Finance只读接口HTTP 400后，旧定时线程每60秒再次运行整批店铺。
- 已止损：数据库先备份，旧服务停止；部署后识别历史失败并把当天计划状态写成`blocked_for_day`，保护至2026-07-16 15:00。服务重启后失败记录没有增加，运行中同步为0。
- 根因修复：90天Finance窗口从最多31个自然日改成最多28个自然日；HTTP 400立即熔断并跳过本批剩余店铺，不再逐店重复相同错误。
- 持久化策略：不可重试4xx当天不再自动请求；429/5xx/网络错误至少退避1小时且最多自动尝试2次；手动同步同样受熔断保护；后台只每5分钟检查本地状态。
- 安全边界：验证仅使用本地模拟和现有失败账本，没有再次请求Ozon；商品写和库存调用均为0。商品图片通道独立运行，没有因工作台短暂停机而被终止。
- 验证：财务专项18项、财务中心及工作台重点回归79项全部通过；运行库和恢复点均为`0600`，SQLite完整性为`ok`。

## 2026-07-14 最新交接：卖点先生成、生图不中断

### 本地视觉基准已落地

- 已完成3张`P000001`本地成品：`benchmark-main-350.png`、`benchmark-detail-kitchen.png`、`benchmark-detail-office.png`，均位于`products/P000001/output/image-quality-benchmark-v1/`。
- 三张图分别回答“这是什么/规格是什么”“使用价值是什么”“适合什么场景”，而不是只展示商品。
- `scripts/image_text_overlay.py`现支持用`||`分隔主标题与副卖点，并支持`auto/top/bottom`落位；详情图会完全替换旧标题区域，再向画面渐隐，防止旧字透出或形成空白框。
- 基准计划`plan.json`已记录完成状态、输出路径、3:4/俄文/无占位框检查，以及Ozon与库存调用次数均为0。
- 回归测试：`python -m unittest tests.test_image_workflow_fix -v`，7项全部通过。

### 当前结论

- 商品分析后的定位步骤现在是图片营销内容的唯一上游：自动保存3至5条可追溯买家卖点、目标买家、痛点、使用场景、情绪触发和视觉方向。
- 图片阶段不再临时猜卖点，也不会因为营销字段不足而停止批次。已有事实优先；不足部分只能生成低风险`支持性推断`，不能发明产品硬参数。
- `copy-ru.json`支持专用`image_copy_ru`和`main_by_sku`；每张图拿到1至3句短俄文卖点，SKU主图分别使用各自容量、材质、颜色和配置。
- 无字场景底图只是内部中间产物，必须叠加卖点并完成文字检查后才能进入工作台成品区；`unknown`不能作为上图文字。

### 当前真实商品验证

- `P000001`定位已从大量`unknown`修正为`completed`，共有5条买家卖点、厨房/办公两个场景和明确科技感电商表达方向。
- 非写入式新图片计划验证为10张：2张SKU专属主图+8张共享详情图；每张俄文短文案均非空、非`unknown`。
- 350ml SKU文案明确316S；400ml SKU明确304S和配盖，不互相套用。
- 没有覆盖当前Ozon已上传图片，没有调用任何Ozon接口；CREATE/UPDATE/只读查询/库存接口均0次。

### 验证与修改文件

- 完整自动测试371项`OK`，114项条件跳过，0失败。
- 主要修改：`scripts/product_positioning_agent.py`、`scripts/image_planner.py`、`scripts/marketplace_content_generator.py`、`scripts/run_batch.py`、`scripts/validate_product.py`、三个相关Schema、测试，以及`P000001`本地定位和图片短文案。
- 下一步由用户确认这3张是否作为视觉基准；确认后把同一规则用于下一件真实商品，禁止对`P000001`重复CREATE。

## 2026-07-14 最新交接：全项目自检、安全加固和运行复核

### 当前结论

- 新增`/Users/apple/Documents/crossborder-ai-factory/scripts/project_self_check.py`，支持一条命令完成离线项目自检和完整测试，报告写入忽略目录`logs/project-self-check.json`。
- 最新自检为`PASS`：16项检查、0失败、0警告；371项自动测试通过，114项条件跳过；当前1个商品通过结构校验，运行批次0、失效任务0、远端待回查队列0、图片通道队列0。
- 服务已重启并恢复到新进程，`http://127.0.0.1:8765/health`和`/workbench`正常；`P000001`为`UPLOADED / 100%`，10张成品图全部能通过真实工作台接口读取。
- 本轮完全离线：网络调用0、Ozon CREATE/UPDATE/只读查询均0、库存接口0。没有清理或改写商品、店铺和运行数据，也没有触碰由另一任务维护的选品关键词模块。

### 已修复问题

1. 工作台图片接口增加商品目录边界校验，阻止图片规划路径越权读取、替换、删除或导出项目文件。
2. 1688图片下载增加可信域名、重定向、协议、端口、Content-Type和25MB大小限制，阻止SSRF和无限下载。
3. Ozon状态聚合区分`PENDING_REMOTE / 99%`和`UPLOADED / 100%`，避免远端未确认时误报完成。
4. Edge插件捕获类目最近/收藏、初始化和打开工作台的连接错误，页面显示中文原因，不再留下未处理Promise异常。
5. 成员访问码升级为带随机盐的PBKDF2-SHA256，同时兼容旧访问码。
6. 插件版本统一为`0.4.7`，中文类目树、搜索索引和规则缓存各7424项；发布ZIP已重新生成并通过完整性校验。

### 本轮修改文件

- `collector/local-ingest/app.py`
- `scripts/image_source_preflight.py`
- `scripts/multi_store_upload.py`
- `scripts/workbench_operators.py`
- `scripts/project_self_check.py`
- `collector/edge-extension/content.js`、`popup.js`及`src/`镜像
- `collector/edge-extension/package.json`
- `tests/test_workbench.py`
- `tests/test_workbench_gap_fill.py`
- `tests/test_stage2_collector.py`
- `tests/test_multi_store_upload.py`
- `tests/test_collector_category_selection.py`
- `tests/test_workbench_operators.py`
- `CURRENT_TASK.md`、`PROJECT_HANDOFF.md`

### 已知剩余风险和下一步

1. 114项条件跳过测试主要依赖已删除的旧商品或可选历史夹具；后续要逐批改为自建临时夹具，不能把“跳过”长期当成覆盖完成。
2. Edge插件本地目录没有安装TypeScript编译器；当前运行JS、TypeScript镜像、Node语法和完整测试均一致，后续发布流水线应固定编译器版本。
3. 根`AGENTS.md`仍包含第一阶段“不开发UI/插件/API”的旧条款，与当前实际项目冲突；在未获得用户明确授权前本轮没有擅自改写治理文件。
4. 下一件真实商品应完整复测一次`采集 -> 选类目 -> 资料优先 -> 最后生图 -> 一次上传 -> 异步回查`，继续验证通用性。上传前必须查重；pending或状态不明确时禁止重传。

## 2026-07-14 最新交接：P000001真实Ozon提交完成

本节是当前权威交接，覆盖后文保留的“等待上传”旧快照。

### 真实结果

- 目标店铺：`zhonglian1`。
- 只读查重：两个`offer_id`均不存在，动作明确为CREATE。
- Ozon商品写接口：1次；一个`/v3/product/import`请求同时包含2个独立商品卡。
- Ozon任务号：`5081018919`。
- `P000001-5931524204563 -> product_id 5482520711`。
- `P000001-5931524204569 -> product_id 5482520644`。
- 异步回查：两个SKU均`success`，店铺`SUCCESS`，商品`UPLOADED / 100%`，远端错误0。
- Ozon审核与校验：两张卡均`approved / validation success / Готов к продаже`；每张卡的1张独立主图和8张共享详情图均已进入Ozon官方CDN。
- 库存字段：0；库存接口：0次。商品导入项中没有`stock/stocks/warehouse_id/quantity`。
- Ozon返回`Нет на складе（无库存）`；这是预期结果，因为项目禁止提交库存。用户需以后在Ozon后台自行加库存，且该商品禁止重复CREATE。

### 本轮修复

- 第一次尝试在写入前失败，未发送Ozon写请求；真实原因是Rich Content允许引用SKU专属主图，但上传解析器错误排除了`variant_main`。
- 上传器现在会把`variant_main`和通用详情图都解析为临时HTTPS图片地址，同时仍把SKU专属主图只放到对应商品卡首图。
- 多店状态聚合现在同步主商品的`upload_status/offer_id/product_id/task_id/shop_name`，并保存合法上传状态历史和完成步骤，工作台不会再出现“远端已成功但本地校验失败”。
- 当前批次`B-846B65169489`已同步为`COMPLETED / 100%`。

### 验证与修改文件

- 完整自动测试：368项`OK`，114项条件跳过，0失败。
- 产品Schema/状态校验：`PASS products/P000001`。
- 工作台服务重载后`/health`返回200。
- 核心修改：`ozon-uploader/ozon_uploader/service.py`、`scripts/multi_store_upload.py`、`tests/test_stage42_ozon_uploader.py`、`tests/test_multi_store_upload.py`及`P000001`店铺发布、状态、上传回执和批次结果文件。

### 下一步

1. 用户在Ozon后台自行补库存并查看买家端商品卡；本项目继续不调用库存接口。
2. 以后修改`P000001`只能做存在性检查后的安全UPDATE，禁止重复CREATE。
3. 用下一件真实商品复测“采集 -> 类目 -> 生成 -> 一次上传”，确认这次修复可重复，而不是只对P000001有效。

## 2026-07-14 最新交接：SKU图片组与容量变体上传语义

本节是当前权威交接，覆盖后文保留的较早运行快照。

### 已完成

- 工作台详情API新增`image_groups`，每个选中SKU独立返回`main_image`和共享`detail_images`；前端按SKU分组展示，不再把所有主图和详情图摊平成一组。
- 图片实际文件仍只有2张SKU主图和8张共享详情图；共享详情图在每组重复展示，但不会复制文件或重复生图。
- 上传器本来已有“每个offer专属主图 + 共享详情图”能力，本轮补齐了本地干跑模板和回归测试，确保上传前就能验证每个offer的图片顺序。
- 官方容量`is_aspect=true`时继续使用`single_card_variants`，每个SKU写入自己的官方容量字典值；无官方容量aspect时使用`separate_cards`，不发明属性。
- `SEPARATE_CARDS_REQUIRED`现在只表示不能合并，不再等同失败。独立卡使用不同型号值和SKU专属名称，防止Ozon误合并。
- 已清除`P000001`旧拆卡失败残留和无效状态历史；当前为`OZON_READY / 95% / manual_ozon_upload`，店铺为`SELECTED / not_submitted`，远端任务号和商品号仍未知。

### 验证结果

- 真实商品安全干跑：2个SKU生成2个独立卡片请求；350 ml与400 ml分别使用自己的主图，之后都是同一套8张通用详情图。
- 干跑`production_blockers=[]`，没有库存或仓库字段，没有网络请求。
- 完整自动测试：359项`OK`，114项条件跳过，0失败。
- 本轮Ozon CREATE/UPDATE/其他写接口均为0；Ozon只读接口0；库存接口0。

### 修改文件

- `collector/local-ingest/app.py`
- `collector/local-ingest/static/workbench.html`
- `collector/local-ingest/static/workbench.js`
- `collector/local-ingest/static/workbench.css`
- `variant-compatibility-checker/variant_compatibility_checker/service.py`
- `ozon-uploader/ozon_uploader/service.py`
- `tests/test_workbench.py`
- `tests/test_variant_image_strategy.py`
- `tests/test_variant_compatibility_checker.py`
- `tests/test_capacity_variant_mapping.py`
- `tests/test_stage42_ozon_uploader.py`
- `products/P000001/status.json`
- `products/P000001/output/store-publications.json`
- `batches/B-846B65169489/batch.json`

### 下一步

1. 重载工作台服务并在页面确认2个SKU图片组。
2. 用户检查成品后自行点击一次最终上传；提交前继续执行防重复和存在性检查。
3. 提交后只读回查每个店铺/offer的`task_id/product_id`；pending或不明确时禁止重传。

## 2026-07-14 最新交接：手动上传状态语义修复

### 真实根因

- 商品没有上传不是因为用户很久以前点击过“安全停止”。`P000001`已完成资料、图片和上传前检查，但本批次`auto_upload=false`，本应停在“等待用户确认上传”。
- 运行器原来先命中`OZON_READY`并提前结束，未把下一步改成`manual_ozon_upload`；批次汇总又无条件写成`COMPLETED / 100%`，造成页面既像已完成又没有远端商品的误导状态。

### 已完成修复

- 手动模式完成生成后统一保存为`OZON_READY / 95% / manual_ozon_upload`，`completed_at`保持`unknown`。
- 批次保存为`AWAITING_MANUAL_UPLOAD / 95%`；只有真正完成远端流程的批次才显示100%。
- 手动待上传批次不再占用运行锁，用户可以从明确的“检查并上传”入口进入商品预览。
- 商品卡显示“待确认上传”和“确认并上传”；预览页保留最后一次“确认修改并立即上传（1家店铺）”按钮。
- 状态Schema已补充运行中的`active_step`；状态迁移校验已支持安全停止、恢复运行和图片步骤断点跳转。
- `portable_kitchen_appliance_fresh`已同步到风格与质检Schema，避免真实商品文件被旧枚举误判。

### 当前真实状态

- 商品：`P000001`，`OZON_READY / final_upload_check / 95% / manual_ozon_upload`。
- 图片：10张已生成，10/10完成硬检查，整体图片质检`pass`。
- 上传前检查：`upload_allowed=true`。
- 批次：`B-2261A4514AF3`，`AWAITING_MANUAL_UPLOAD / 95% / auto_upload=false`。
- Ozon本地状态：`upload_status=not_started`，`task_id/product_id/offer_id`均为`unknown`。
- Ozon CREATE 0次、UPDATE 0次、其他商品写接口0次；库存接口0次。
- 工作台服务健康检查200；真实浏览器已验证“待确认上传95% -> 确认并上传 -> 预览检查页”导航，未点击最终上传。

### 最近测试与修改文件

- 完整自动测试356项全部`OK`，114项按条件跳过，无失败。
- 核心修改：`scripts/run_batch.py`、`scripts/pipeline_runtime.py`、`collector/local-ingest/app.py`、`collector/local-ingest/static/workbench.js`、`templates/status.schema.json`、`templates/style-profile.schema.json`、`templates/qc-report.schema.json`、`scripts/validate_product.py`及对应批次/工作台/状态测试。
- 本地运行数据已纠正：`products/P000001/status.json`、`batches/B-2261A4514AF3/batch.json`、批次结果文件和根`batch-result.json`。

### 下一步

1. 用户在预览检查页确认成品后，主动点击一次最终上传按钮。
2. 系统按上传前只读存在检查的结果选择CREATE或安全UPDATE，不重复CREATE。
3. 提交后逐店保存`offer_id/task_id/product_id/payload_hash`并异步回查；pending或状态不明确时禁止重传。

## 2026-07-14 工作台后台常驻修复

- 故障表现：主电脑访问`http://127.0.0.1:8765/workbench`显示`ERR_CONNECTION_REFUSED`。
- 真实原因：此前服务属于Codex临时进程，任务结束后被回收；旧启动脚本使用前台`exec`，关闭终端也会关闭工作台。
- 修复方案：启动器使用`nohup`启动独立守护脚本，守护脚本负责运行Uvicorn并在异常退出后2秒重启。服务进程已脱离终端，父进程为系统PID 1。
- 当前验证：端口8765正在监听，`/health`返回200，浏览器已成功加载工作台及商品接口；结束Codex任务不再关闭服务。
- 修改文件：`启动工作室工作台.command`、`scripts/run_workbench_service.sh`、`tests/test_workbench_launcher.py`、`README.md`、`docs/工作室局域网使用说明.md`。
- 完整测试357项全部`OK`，114项按条件跳过；Ozon写接口0次，库存接口0次。

## Git基线

- 项目根目录：`/Users/apple/Documents/crossborder-ai-factory`
- 本地分支：`main`
- 基线提交：`a8f8b58df5f3e06f7f924e45b348a555d3fc0375`
- 基线标签：`baseline-before-category-selection`
- 基线提交信息：`baseline: current crossborder-ai-factory before category workflow update`
- 工作台与成员隔离阶段提交：`a6491560c3033af5b41153b11d89437189774e81`（`feat: simplify workbench and isolate operator data`）。
- 清理旧采集箱和重复发布镜像提交：`7d54daf`（`refactor: simplify core workbench maintenance`）。
- 一键启动、类目搜索和7424类目离线规则提交：`aadf80b`（`fix: keep workbench and category selection available`）。
- 基线包含327个文件，约166.15 MiB，主要体积来自必须保留的Ozon本地规则/类目缓存。
- 没有配置远程仓库，没有push，没有上传GitHub。

## 敏感信息处理

- `.env`、`.env.*`（模板除外）、真实店铺密钥、Token、Cookie、密码、本地访问码、数据库、运行队列、商品数据、图片缓存、日志和临时文件均已加入`.gitignore`。
- `ozon-adapter/.env.zhonglian1`含真实本地店铺凭证：已保留使用、未加入Git、未输出内容。
- `config/lan-access.json`含本地访问码：已保留使用、未加入Git。
- `ozon-adapter/shops.json`只含环境变量引用和店铺元数据，可纳入版本控制。
- 测试中的`client-secret`和`api-secret`为假数据，不是生产凭证。

## 已执行的数据清理

旧交接记录曾描述“全部删除”，现已被2026-07-16规则替代：旧商品只做本地归档，店铺配置继续使用。因此当前不做物理删除：

- `products/`内P000001–P000005完整资料保留7天恢复期。
- `batches/`内历史批次保留7天恢复期。
- `runtime/recovery-archive/`保存归档摘要；SQLite保留不可变远端字段。
- 运行日志、PID、锁、队列、图片缓存、缩略图缓存、临时公网通道状态和旧发布ZIP。

保留：

- Ozon店铺配置和本地密钥。
- 局域网访问配置。
- Ozon中文类目树、属性规则、字典值、`is_aspect`和规则缓存。
- 全部源码、测试、Schema、迁移、规则模板、Skills、README和交接文档。

## 本阶段功能完成

### 资料优先、最后生图

- 批次已从旧的“先图片、后平台资料”调整为“商品只分析一次，先完成标题/简介/30标签/SKU/价格/全部类目属性，再生成全部逐图提示词，最后统一生图”。
- `marketplace_content`和`field_completion --pre-image`在图片前完成；图片完成后由`final_upload_check`生成Rich Content并执行最终上传检查，不重复分析商品。
- `image-plan.json`现在保存最终标题、描述、标签、最终属性、SKU分组和价格的可追溯快照，并要求全部逐图提示词先生成完成。
- 图片生成任务只消费最终资料和已保存提示词，不再重新猜测类目或补字段。
- 提示词和图片质检均禁止空白圆角框、空文字面板、占位卡片和通用模板版式。
- 旧断点兼容逻辑会把没有远端写入的旧“图片优先”任务安全迁移到新顺序；已有明确远端状态的商品不会被重置。

### 任务式工作台

- 主入口：`我的采集箱 / 需要我处理 / 已上架商品`。
- 店铺设置和系统设置只对负责人显示。
- 每个商品卡只有一个随状态变化的主要动作，减少寻找功能的时间。
- “需要我处理”只集中显示关键问题、明确失败和已生成待上传商品。
- 电脑端1280×720和手机端390×844真实浏览器验收通过。

### 成员商品隔离

- 新商品采集时保存`input/owner.json`。
- 成员访问码以SHA-256哈希保存；访问码只在创建/重置时显示一次。
- 每个成员只能看、改、运行、停止、删除、导出和上传自己采集的商品。
- 批次使用`batches/<batch_id>/owner.json`绑定所属成员。
- 通知、商品列表、详情、图片、店铺选择、发布记录、日志、风险和导出均由后端按成员过滤。
- 负责人可以管理成员、店铺和全局设置，但没有跨成员查看商品的权限。
- 成员可以选择全部已配置店铺，但不能修改店铺凭证和全局设置。
- 删除仍有商品的成员会被阻止，避免商品成为无主数据。

### 通知和关键提问

- 工作台每15秒读取当前成员的通知。
- 支持浏览器/系统桌面通知；点击通知会聚焦工作台并打开对应商品。
- 只有商品身份、SKU/变体/颜色映射、结构、配件数量、真实参考图等关键事实冲突或缺失时才创建问题。
- 普通可选属性未知不会打断任务。
- 回答保存到`input/operator-guidance.json`，只用于对应问题并可追溯。

### 图片视觉与速度

- 支持整组简单风格意见和单图意见。
- 意见进入风格选择、图片规划和生图提示，但不能覆盖真实性硬规则。
- 类目、原始图、视觉意见和关键输入参与哈希；未变化的步骤复用缓存。
- 风格变化只从风格选择阶段向后失效，不重复分析无关步骤。
- 仍保持每SKU真实主图、6–8张共享详情图、非白底、逐图失败重试和已完成图保留。

### Edge采集插件

- 版本升级到`0.4.7`。
- 页面使用“成员访问码（决定商品归属）”说明。
- 继续要求SKU不超过10个和最终Ozon类目必选。
- 中文类目树和本地缓存继续使用，不在每次采集时重新加载Ozon。
- 修复类目树可见但输入搜索无反馈：现在输入会立即显示搜索状态，并支持点击“搜索”或按回车。
- 7424个最终类目的必填属性和`is_aspect`已生成一份主程序与插件共用的精简离线规则缓存；首次选择新类目也不需要临时查询Ozon，仓库不保留第二份重复缓存。
- 当前官方批量规则库不含所有字典值；缺失值明确保存为`unknown`，插件另行缓存最近12个已加载类目的完整规则和字典值。
- 主电脑上的1688采集请求可继承负责人身份；局域网成员设备仍必须使用各自访问码，任意外部网页仍不能继承负责人权限。
- 插件源码只保留在`collector/edge-extension/`；`release/edge-extension/`重复镜像已删除。
- `scripts/package_edge_extension.py`从唯一源码生成版本化发布ZIP，避免两份代码不同步。

### 清理与本地安全补漏

- 已删除独立旧`/inbox`静态页面及路由，工作台成为唯一商品操作界面；供当前工作台调用的`/api/inbox/*`接口未删除。
- 外部网页对`127.0.0.1`发起跨站请求时不能再自动继承负责人身份。
- 本机同源工作台仍可自动识别负责人；成员设备继续使用各自访问码，Edge采集插件兼容方式未改变。
- 局域网文档不再引用不存在的`启动工作室工作台.command`，并明确每个成员只能看到自己的商品。
- 根目录现已提供真实可用的`启动工作室工作台.command`：自动寻找可用Python、检测端口和健康状态、避免重复启动，并以`0.0.0.0:8765`供本机与局域网使用。

## 当前数据与接口状态

- 当前有1件真实测试商品`P000001`、2个SKU；最终类目为`17039627 / 94982`。
- 历史批次3个；当前没有运行中的批次，`P000001`已安全停止在`marketplace_content`，工作台显示60%。
- 当前商品已生成最终标题、简介、30个标签、2个SKU价格、43项最终类目字段记录和10份商品专属逐图提示词；14项有值、29项为`unknown`，3项必填属性全部填写。
- 上一轮不达标的生成图、图片锁和旧图片质检结果已清除，1688原始图与原始输入保留；当前生成图片数为0。
- 店铺`zhonglian1`配置保留，凭证不在页面和Git中暴露。
- 本阶段Ozon CREATE 0次、UPDATE 0次、其他商品写接口0次。
- 库存接口调用0次。
- 未生成远端task_id/product_id，也没有pending任务。

## 最近验证

- 资料优先流程改造后完整自动测试345项全部`OK`；230项实际执行，115项依赖历史运行商品的测试跳过。
- 真实工作台API确认：`STOPPED / 60% / next_action=marketplace_content`，10个图片位均为`WAITING`且各自已有最终资料驱动的提示词。
- 真实商品`P000001`已验证预生图字段补全：30个标签、43项最终字段记录、3项必填全部填写；图片阶段尚未运行。

- 本轮完整自动测试343项全部`OK`；228项实际执行，115项依赖历史运行商品的测试跳过。
- 真实工作台浏览器验证：采集箱商品卡显示当前步骤与71%进度；点击“查看进度”可进入预览页；预览页显示10张计划图中的逐图状态和已完成数量。
- 商品处理无响应根因已修复：便携式厨房小家电缺失图片风格规则；现在命中`portable_kitchen_appliance_fresh`，置信度0.94，并从失败断点恢复。
- Codex图片子任务固定使用项目Python环境，避免其他Python缺少Pillow导致图片叠字环节卡住；已完成图片不会重做。
- 只包含本轮一键启动、类目搜索和离线缓存修改的干净临时副本：323项全部`OK`；203项实际执行通过，120项跳过。
- 当前整个工作区自动发现339项并全部`OK`；219项实际执行通过，120项历史运行商品测试跳过。
- 跳过项均依赖已经按用户要求删除的旧商品运行目录；本轮没有恢复旧数据。
- 新增成员权限、数据隔离、关键问题、视觉意见和工作台测试全部通过。
- Python语法检查通过。
- 真实工作台API：服务健康、负责人会话、空商品、空通知、保留店铺均返回正确。
- 真实浏览器：三个主入口、系统设置、成员区、店铺状态、手机菜单均正常；控制台0错误/警告。
- 提交`a6491560c3033af5b41153b11d89437189774e81`已在不包含未提交市场情报模块的纯净临时工作区再次启动并验证健康、会话和空商品接口。
- Edge插件`0.4.7`发布ZIP通过`unzip -t`完整性检查，包含7424类目离线规则，大小约1.6 MiB，SHA-256为`97c372007e967c3ace621a27e0c21fb31e67db1002d6a34110971566a8d16602`，重复打包哈希一致。

## 尚未完成与商业化判断

- `P000001`仍在生成剩余详情图，尚未完成第一件真实可售商品的全链路上传和审核结果。
- 真实多店写入、逐店只重试失败店和异步回查需要用新商品验收。
- 图片质量需要用新商品与用户手动流程做真实对照，不能只靠自动QC判定。
- 120项历史集成测试需逐步改造成自带合成夹具。
- 工作台前端仍有不可达的旧页面渲染代码；选品与关键词模块正由另一对话并行修改同一组前端文件，本轮为避免覆盖对方工作暂不删除。
- 商业化前建议完成30个真实商品、至少5个类目、连续7天无数据丢失；重复CREATE、库存接口和成员越权必须保持0。
- 当前更适合作为工作室内部试跑版，尚不适合直接售卖给外部用户。

## 下一步动作

1. 用户点击一次“运行任务”，让`P000001`按新顺序从`marketplace_content`继续。
2. 文字、SKU、价格和属性完成后生成新图片；需要时只重做指定单图，不重跑商品分析和资料生成。
3. 本地预检和只读存在检查都明确后，再由用户主动执行真实上传。
4. 保存逐店逐SKU远端记录并异步回查到可售或明确失败。
5. 第一件成功后开始30商品工作室试跑，并同步清理历史测试夹具依赖。

## 2026-07-13 最新修复交接

- 旧表现：失败商品被错误显示为“需要人工审核”，商品行点击无反馈，重新运行还会要求第二次确认。
- 真实原因：`P000001`已锁定厨房搅拌机类目，但风格规则未覆盖便携式榨汁杯，`image_plan`按真实性门禁正确停止。
- 已修复：补充专属风格/图片结构规则；失败归类为“处理失败”；展示失败步骤、原因和百分比；商品卡与按钮均可进入详情；已授权任务从断点继续。
- 图片运行环境：`scripts/run_batch.py`为Codex子任务注入项目Python和`CAF_PYTHON_BIN`，图片辅助脚本不得再误用Homebrew Python。
- 当前运行：计划2张SKU主图+8张共享详情图；页面会显示已完成数量，已完成文件与硬检查结果保留。
- 修改范围：`rules/style_selector_rules.json`、`rules/style_profiles.json`、`rules/image_structure_rules.json`、`scripts/run_batch.py`、工作台后端/前端及相关自动测试。
- 安全计数：Ozon写接口0次，库存接口0次。

## 永久业务限制

- 不提交库存字段，不设置`stock=0`，不调用库存接口。
- pending、上传中、审核中或状态不明确时禁止重传。
- 不重复CREATE；明确存在的商品只能安全UPDATE。
- 单商品最多10个SKU。
- 单商品或单店失败不阻塞整个批次；只重试明确失败店铺。
- 每店独立保存`offer_id/task_id/product_id/payload_hash`和状态。
- 类目由采集阶段用户最终选择，运行中不得替换。
- 属性和变体逻辑必须使用当前类目规则快照、字典值和`is_aspect`。
- 不虚构材质、承重、认证、功能和配件；估算值必须有标记、置信度和来源。

## 工作区注意事项

以下为另一对话正在处理的选品与关键词内容，本轮未修改、未删除、未纳入本轮提交：

- `docs/Ozon选品与关键词模块方案.md`
- `market-intelligence/`
- `scripts/import_ozon_bestsellers.py`
- `scripts/import_ozon_search_queries.py`
- `scripts/probe_ozon_market_sources.py`
- `scripts/refresh_ozon_market_daily.py`
- `templates/market-source-status.schema.json`
- `tests/test_market_intelligence.py`
- `.gitignore`中的对应运行数据忽略规则
- `collector/local-ingest/app.py`、`static/workbench.html`、`static/workbench.js`和`static/workbench.css`中的对应未提交修改

## 2026-07-13 对外商业化自查补充

- 评估目标：客户电脑本地运行；面向3–10名不懂技术的Ozon卖家，必要时远程协助安装。
- 功能清单完成度约75%，部分实现折算后约86%；对外商业化成熟度约40%，不能用功能数量代替真实可售闭环。
- 当前没有商品、批次或远端任务，因此生成、图片、真实上传、失败店重试和异步回查无法做本轮页面验收。
- 出售前必须完成：第一件真实可售商品；本地接口安全收紧；一键安装/启动；依赖环境锁定；备份恢复/诊断/版本检查；历史测试自带夹具；真实图片质量对照；明显中文类目误译修正。
- 一键启动已经补齐；一键安装、依赖环境锁定、备份恢复和诊断仍未完成。
- 可删除或合并：旧`/inbox`页面、重复的`release/edge-extension`源码镜像、客户不可达的旧首页/Skill/经验/高级日志渲染器、静态AI权限中心。
- 选品与关键词模块应暂标Beta而非删除；主流程验证前不要继续扩大范围。
- 当前完整工作区在项目实际Python环境下测试332项：212项执行通过，120项跳过。
- 商业化审计截图及逐步记录：`logs/commercial-audit-20260713/`。
- 用户已明确商业化不是当前重点；近期只优先验证真实商品闭环、图片质量、上传稳定性和操作效率。

## 2026-07-13 最新交接：属性自动分析与图片缓存修复

### 已完成

- 类目字段补全已从“只复制原文”升级为“原始事实 + 低风险结构推断 + 风险门禁”。每个自动值保存来源、证据、推断类型和置信度，人工修改仍拥有最高优先级。
- 当前真实商品`P000001`共有43项类目字段，22项有值、21项明确未知、3项必填全部完成。工作台已实测显示：手持式、无线、非旋转杯体、非行星结构、金属杯体、不锈钢外壳、标题模板型号和俄文包装清单。
- SKU事实被合并进包装清单：每个变体1个榨汁杯，只有400 ml变体明确包含杯盖。没有把SKU差异错误写成全商品共同事实。
- 功率、档位、保修、认证、保护系统、标识码、HS编码和电源线长度没有可靠来源，仍保存为`unknown`。
- 修复商品尺寸和包装尺寸混用；运费继续只使用包装数据。
- 修复工作台图片旧缓存：新文件使用内容版本地址并禁止缓存；未通过硬检查的文件显示质检状态，不再显示完成。
- 删除并排队单独重做`detail-004`、`detail-007`、`detail-008`；其他已通过图片和前置资料全部保留。

### 当前状态

- `P000001`：`STOPPED`，进度78%，下一步`image_generation`。
- 页面图片状态：2张主图+5张详情图`PASS`；3张详情图`RETRYING`。
- 继续运行时只重做3张问题图，不重复商品分析、标题、简介、30个标签、SKU、价格和属性。
- 工作台服务运行在`0.0.0.0:8765`，本机健康检查200。
- Ozon CREATE 0次、UPDATE 0次、其他商品写接口0次；库存接口0次。

### 最近测试

- 完整测试348项全部`OK`；233项实际执行通过，115项依赖历史运行商品的测试跳过。
- 真实浏览器验证了新增属性值、来源标签、图片`PASS/RETRYING`状态和停止后的78%进度。

### 本阶段修改文件

- `ozon-field-completion/ozon_field_completion/service.py`
- `scripts/run_batch.py`
- `collector/local-ingest/app.py`
- `collector/local-ingest/static/workbench.js`
- `config/ozon-attribute-translations-zh.json`
- `tests/test_source_backed_attribute_inference.py`
- `tests/test_workbench_gap_fill.py`
- `CURRENT_TASK.md`
- `PROJECT_HANDOFF.md`

### 下一步

1. 用户继续运行`P000001`时，仅生成3张被退回的详情图。
2. 图片通过硬检查后执行最终资料校验；缺少依据的风险字段仍不阻塞上传，平台必填字段除外。
3. 上传前继续执行本地防重复检查和远端只读存在检查；只有状态明确才允许写入。
4. 完成第一件真实可售商品后，再验证逐店异步结果和只重试失败店铺。

### 2026-07-14 最新修复交接

- 通用 Ozon 属性标准化已落地：上传草稿只使用标准字典值，不携带来源说明；非必填无匹配直接不填。
- `P000002` 的 `6383/Материал` 已修复为 `Нержавеющая сталь`，上传前校验 PASS，未发生任何 Ozon 写入。
- 修复标题生成函数空返回导致的商品资料阶段失败。
- 风格选择改为商品资料足够时自动选择，不再等待人工确认。
- 图片任务旧版整套超时造成“已生成部分图片仍判失败”；已修改为保留完成图片、跳过超时图片、继续后续阶段的断点逻辑。

### 2026-07-16 联网大模型不可用处理（当前权威）

- 联网大模型不可用时保持当前`next_action`和全部断点，状态显示`waiting_for_recovery`，默认约30秒自动重试。
- 本地备用分析已删除；禁止用本地规则或简化分析继续生成俄文资料、属性和图片。
- 等待期间不计商品失败、不推进步骤、不调用Ozon写入、远端回查或库存接口。

### 2026-07-14 P000004 分析失败恢复（历史记录，兜底方案已作废）

- `P000004` 的“分析失败 1%”根因是Codex子进程访问服务时收到`403 Forbidden`，不是商品源数据错误，也没有触发Ozon写接口。
- 当时曾加入本地事实兜底；该方案已于2026-07-16按用户最新确认删除，不再使用。
- 兜底分析已通过商品分析Schema校验，保留3个SKU、标题中的双开门/免弯腰/带盖/大容量等事实、人工确认尺寸重量及未知字段；不会伪造材质、容量、承重、认证或库存。
- `P000004` 当前已恢复并完成`category_match`、`variant_rules`，`next_action=measurements`。后续运行继续按断点处理，不重复已完成分析，不重复CREATE。
- 维护注意：如果工作台再次出现“分析失败1%”，先看`products/<id>/logs/full-pipeline.log`；若是403类服务故障，系统应显示等待并自动重试联网大模型；若是源数据/Schema错误，则保留硬失败并展示具体字段。

## 2026-07-14 当前交接：商品生产测试问题修复收尾

### 当前结论

核心主流程的已知阻断已修复并通过离线项目自检。资料链路现在是：

`1688事实 -> 锁定类目 -> 一次分析 -> 标题/简介/30标签/SKU/价格/类目属性 -> 图片规划 -> 生图 -> 图片质检 -> 上传前检查 -> 多店上传/异步回查`

单商品失败不会再阻塞整批；图片、属性、SKU和上传状态均保留可追溯证据。选品关键词模块由另一对话维护，本轮没有改动其业务实现。

### 已完成修复清单

1. 真实SKU颜色识别和逐SKU映射，避免所有变体串色。
2. 每个SKU独立主图、共享详情图；标题和提示词不再继承错误颜色/多颜色描述/错误配件。
3. 结构特征与独立配件分开，未知配件不虚构。
4. 人工确认尺寸重量保持来源证据，Ozon属性仍符合现有Schema。
5. 采集器过滤平台价格免责声明，并补读结构化商品属性。
6. 类目属性预检不再要求图片前不存在的草稿文件；按锁定类目快照和`is_aspect`执行。
7. 预检移除库存/仓库误报；库存字段和库存接口仍为0。
8. 生产模式环境继承、失败商品隔离、断点恢复和旧状态容错。

### 验证与安全计数

- 离线项目自检：`PASS`，15项检查，0失败、0警告。
- `P000002`、`P000003`、`P000004`结构校验通过；`P000002`的旧失败重试历史不会再覆盖其后续成功上传状态。
- 核心回归测试42项通过、3项条件跳过（变体、采集器、容量变体、图片流程、批次确认）；全量测试本次发现359项，其中15项旧断言失败、38项因已删除旧商品夹具报错、22项跳过，集中在`P000005/P000009`及旧价格/图片数量/风格预期，不恢复真实商品数据。
- 本轮Ozon CREATE 0次、UPDATE 0次、只读查询0次、库存接口0次。

### 当前未完成

- 尚未用下一件新商品做完整图片质量和端到端重复性验收。
- 全量测试中的历史夹具需要改成自带临时夹具，避免“删除运行数据后测试报错”。
- Edge插件本地目录仍未安装TypeScript编译器；运行JS语法、源码镜像和离线自检一致，发布前可在固定构建环境编译。

### 修改文件

本阶段核心修改：

- `variant-compatibility-checker/variant_compatibility_checker/service.py`
- `ozon-field-completion/ozon_field_completion/service.py`
- `scripts/marketplace_content_generator.py`
- `scripts/image_planner.py`
- `scripts/run_batch.py`
- `scripts/validate_product.py`
- `ozon-adapter/ozon_adapter/service.py`
- `scripts/ozon_metadata_matcher.py`
- `collector/edge-extension/content.js`
- `collector/edge-extension/src/content.ts`
- `CURRENT_TASK.md`

### 下一步动作

1. 采集下一件真实商品并最终选择类目、SKU。
2. 点击一次“运行任务”，让系统无人值守完成文字/属性后再生图；失败只重试失败图片或商品。
3. 图片质检通过后，用户主动点击一次上传；系统先做只读查重，pending/未知状态禁止重传。
4. 逐店保存`offer_id/task_id/product_id/payload_hash`并回查，失败店铺独立重试。
5. 把旧测试改为临时夹具，保持测试覆盖而不恢复或提交真实商品数据。

## 2026-07-15 自动连续流程修复

### 本轮完成

- 工作台批次创建和单商品续跑统一为自动连续流程：一次运行授权后，自动完成文字、图片、硬错误快检、上传前检查和Ozon上传，不再进入旧的人工确认边界。
- 旧的`config/workbench-settings.json`手动模式会在读取时自动归一为自动连续流程，避免历史配置重新造成断点。
- 店铺选择默认全部已验证店铺，允许在运行前取消某些店铺；未选店铺会在启动前明确提示。
- `CONTENT_GENERATED`、`PRICED`、`IMAGES_GENERATED`和授权失败断点会直接从`next_action`入队，保留已完成步骤，不重复CREATE。
- 前端不再弹二次确认，按钮明确显示将自动生成并上传；工作室成员点击继续也走同一后端路径。

### 当前状态

- P000004已确认根因：旧批次结束后没有活动worker，状态停在`78% / image_generation`；本轮修复已部署，尚未替用户重新触发真实Ozon上传。
- 工作台服务已重启，`/workbench`返回HTTP 200，设置API返回`auto_mode_enabled=true`。
- 本轮验证：核心测试61项通过，项目自检15项PASS；Ozon CREATE/UPDATE/只读查询0次，库存接口0次。

### 下一步

1. 用户刷新工作台后，在P000004点击“继续生成”；后端应直接进入图片生成队列。
2. 等图片完成后自动执行上传；失败商品隔离并保留原因，其他商品继续。
3. 如需真实上传，仍遵守pending/状态不明确禁止重传、每店独立记录和库存接口0次。

## 2026-07-15 错误处理交接

本轮完成错误提示和人工修改入口：

- `collector/local-ingest/app.py` 新增统一错误说明，将网络、图片、类目属性、价格尺寸、上架条件和资料生成失败转成用户能看懂的标题、原因、动作和目标面板；技术原文仍保留。
- `collector/local-ingest/static/workbench.js` 在商品卡和预览检查页加入“立即修改”；点击后自动进入详情并定位到相关面板/字段。失败图片进入图片面板，类目属性进入类目面板，价格进入价格面板，资料进入资料面板，上传条件进入店铺面板。
- `collector/local-ingest/static/workbench-future.css` 增加错误说明、技术详情、操作按钮和字段高亮样式。
- 系统通知、风险列表、运行中提示统一使用白话错误说明，不再直接把`Failed to fetch`或内部步骤异常当作用户提示。

验证结果：前端语法检查通过；后端编译通过；工作台相关 unittest 62项通过；项目自检15项 PASS；本轮Ozon CREATE/UPDATE/只读查询/库存接口均为0。

下一步用一件真实失败商品验证按钮定位和字段保存，随后再处理历史测试夹具，不恢复商品运行数据。

### P000004 断点按钮补漏

P000004的状态是`CONTENT_GENERATED / 78% / next_action=image_generation`，之前因状态映射缺少该分支而没有“继续”按钮。现已将中间断点状态统一映射为“继续生成”，点击后进入预览检查并可从断点继续，不会重做已经完成的资料，也不会触发重复CREATE。

### 2026-07-15 继续任务实际阻塞修复

- 之前“继续”点了没有反应的直接原因不是商品生产失败，而是前端把 `CONTENT_GENERATED` 当成活动运行状态，按钮一直是禁用状态；P000004 实际没有活动 worker。
- 已修复运行状态判断：只有真实 `active_step`、排队或上传状态才显示运行中；已授权的中间断点可直接继续。
- 已修复后端断点恢复：`CATEGORY_MATCHED`、`PRICED`、`CONTENT_GENERATED`、`IMAGES_GENERATED` 在 `task_authorized=true` 时直接从下一步启动，不再要求第二次确认；手动生成不需要店铺，自动上传仍强制店铺校验。
- 当前 P000004 仍是 `78% / image_generation`，图片生成、图片质检、最终上传检查和Ozon上传尚未执行；Ozon CREATE/UPDATE/只读查询/库存接口均为0。
- 采集箱对中间断点显示“待继续”，只有真实 worker/排队状态显示“生成中”，避免误以为任务卡死或仍在运行。
- 已授权断点不再弹出第二次确认；点击“继续生成”即启动恢复，只有真正的Ozon上传保留上传确认。
- 验证通过：61项工作台和局域网相关测试、前端语法、后端编译；工作台服务重启后HTTP 200。
## 2026-07-15 图片进度更新

- 修复了图片生成阶段“实际已生成但界面仍停在 78%”的问题：工作台现在显示已完成/计划槽位数量和增量进度。
- 图片阶段单商品总时限统一为 10 分钟；超时不会清空已生成图片，只跳过未完成槽位并继续后续质检/上传准备。
- P000004 当前批次 `B-3A8AE7EFF678` 已生成 6/11 张，仍在旧批次进程中收尾；本次 Ozon 写接口调用 0、库存接口调用 0。

## 2026-07-15 第7、8张图片恢复完成

- 第7、8张及其余缺失详情图已补齐；P000004 当前11/11张图片存在并通过硬错误快检，图片质检报告已重建并通过。
- 修复了过期图片质检报告导致的误阻塞：每次进入图片质检都会基于最新硬校验结果重建报告。
- P000004 当前状态为 `OZON_READY`，等待人工上传；本次未调用任何Ozon接口，CREATE/UPDATE/只读查询0次，库存接口0次。
- 下一步：用户确认后才执行上传；若失败只重试失败店铺/失败步骤，不重复CREATE。
- 继续按“先文字、后图片、最后上传”验证下一件真实商品。

## 2026-07-15 故障演练结论

### 已确认的防线

- 来源、类目、SKU上限、必填属性、价格、包装尺寸重量和30标签在上传前校验；不合格商品不写Ozon。
- 图片阶段按槽位计数；缺图、空白图、错误SKU、错误结构、白底/无卖点文字和过期质检报告会被拦截。质检报告现在每次从最新硬校验结果重建。
- 图片超时、单图失败、AI传输失败、单店失败和安全停止都按“保留断点、单项重试、其他商品继续”处理。
- pending/未知远端状态禁止重传；不提交库存字段、不调用库存接口；同一商品按offer/task/hash防重复。

### 当前仍需重点观察

- 真实新商品的完整端到端运行尚未验收；图片质量仍需按俄罗斯电商视觉标准人工抽检。
- 服务停止、LAN地址/端口不可达、插件指向旧地址、`.venv`未启用仍会导致“连接失败/任务不动”。
- 全量测试尚未全绿：虚拟环境共376项，23失败、34错误、22跳过，主要来自已删除商品夹具和旧断言；核心目标回归70项通过。

### 当前商品安全状态

P000004 当前为`PENDING_REMOTE / task_id=5095805941`，只允许只读状态回查，禁止再次上传。故障演练期间检测到旧工作台批次`B-D0A8CD0536E2`自动产生1次写入（店铺`zhonglian1`）；之后没有继续写入，库存接口调用为0。

> 注：以上是多店铺补写前的历史快照；下方“当前交接：多店铺同步已补齐”才是当前各店最新状态和写入计数。

### 下一步动作

1. 先只读回查P000004远端状态，不重传。
2. 用新采集商品做一次无写入演练，逐项记录错误是否被拦截或自动跳过。
3. 修复历史测试夹具，再做一次全量测试；不恢复已删除商品数据。
## 2026-07-15 当前交接：多店铺同步已补齐

### 已完成

- 修复批量重试逻辑：当用户明确指定失败店铺时，不再因为同一商品已有历史写入而整商品跳过；`PENDING_REMOTE`、成功和状态不明确的店铺仍然禁止重传。
- 工作台新增“并行重试全部失败店铺”按钮，一次启动一个批次处理全部可安全重试店铺，店铺失败隔离，支持后续只重试单店。
- P000004的三家已选店铺当前均为`PENDING_REMOTE`：`zhonglian1` task `5095805941`、`zhonglian2` task `5096278947`、`zhonglian5` task `5096282804`。
- 本次真实Ozon写接口累计3次，每店1次；没有重复CREATE，没有提交库存字段，库存接口调用0次。

### 当前未完成

- 三个任务仍需只读回查，等待Ozon最终审核状态和`product_id`；在此之前严禁再次上传。
- 全量测试仍有历史夹具失败，核心回归测试保持通过；后续应继续清理测试夹具，不恢复商品运行数据。

### 下一步动作

1. 只读查询`5095805941`、`5096278947`、`5096282804`，写回各店独立状态。
2. 验证工作台是否显示“三店处理中”及每店任务号，确认不会出现“逐店等待”或整批卡死。
3. 只有远端明确失败且本地记录允许重试时，才对该失败店单独重试；其余状态不触发任何写接口。

### 2026-07-15 只读回查结果

- `zhonglian2`已确认导入：商品ID `5501114713`、`5501118278`、`5501061716`；Ozon返回已审核/待库存状态，因此在该店后台应能按这三个商品查看，库存仍未由本工具提交。
- `zhonglian1`任务`5095805941`仍在处理中，暂时不会出现在可售商品列表。
- `zhonglian5`任务`5096282804`暂未返回商品条目，仍保持处理中；没有错误证据，不允许重传。
- 本次只读回查写接口0次、库存接口0次；累计真实写接口仍为3次，未新增任何CREATE。

## 2026-07-15 断点与图片空白框修复

### 已完成

- 图片阶段现在按实际落盘文件和旧/新方案契约计数：旧方案没有像素锁时不再因缺少锁清单把进度卡在78%；新方案仍严格要求像素锁。
- 图片质检新增空白占位框拦截。发现大面积空面板时写入 `empty_placeholder_panel`，工作台用白话说明原因，只重做失败槽位，合格图片不动。
- 图片质检报告Schema允许保存检测证据，便于查看是哪一张图、哪一块区域触发拦截。
- 类目属性继续只写入真实Ozon字典值；包装清单不把商品自身结构误当独立配件。

### 验证

- 项目自检15项PASS。
- 定向回归72项通过、2项条件跳过；空白框检测2项通过，现有49张本地PNG未误报。
- 全量测试379项：23失败、20错误、35跳过，剩余为已清理商品资料相关的历史测试夹具和旧断言，未恢复这些商品数据。
- 本轮Ozon CREATE、UPDATE、只读查询、库存接口均为0。

### 仍需做

1. 用新商品跑一次真实图片链路，人工抽检主图卖点文字、详情图信息密度和SKU差异。
2. 把历史测试统一改成临时商品夹具，避免删除运行数据后出现假失败。
3. 继续保持：pending/状态不明确不重传、不提交库存、不调用库存接口、失败店铺独立重试。

### 2026-07-15 超时显示与低清SKU断点修复

- 当前没有运行中的批处理或图片子进程；`P000003`原先的“处理很久”是旧 `active_step` 残留和失败状态，不是真实后台任务。
- 829×576 的SKU原图不再因为略低于推荐600px而直接停止。512–599px作为“带警告可用”参考图继续走 `edit_real_image`；低于512px才阻断，且不放大、不抠图、不复制像素。
- `P000003`已恢复为 `CONTENT_GENERATED`，`next_action=image_generation`，清除过期活动步骤；用户点击继续后从图片阶段开始。

### 2026-07-15 最终复核

- `P000003` 产品状态校验已通过；旧的低清参考图失败原因保留在历史记录，恢复记录追加到最新状态，避免界面继续显示“处理中”或停在失败断点。
- 当前无后台批次运行；项目自检 15/15 PASS，定向回归 22 项通过、2 项条件跳过；本轮 Ozon 写接口、只读查询和库存接口均为 0。

### 2026-07-15 P000004 异步任务延迟复核

- `zhonglian1 / 5095805941` 已只读回查 8 次，仍为 `processing`，3 个 SKU 均无商品 ID、无错误。
- `zhonglian5 / 5096282804` 已只读回查 5 次，仍为 `processing`，返回条目为 0、无错误。
- `zhonglian2` 已成功，说明商品资料和主上传流程可用；当前差异集中在两个店铺的 Ozon 异步导入队列。禁止在 pending/unknown 状态下重传，继续只读回查即可。

## 2026-07-15 插件连接修复（当前最新）

- 根因：1688 页面为 HTTPS，旧内容脚本直接请求 `http://192.168.x.x:8765`，被 Edge 按 Mixed Content 拦截。
- 修复：新增扩展 service worker `collector/edge-extension/background.js`，内容脚本的工作台 API 请求统一经扩展后台转发；仍校验主电脑为本机/局域网地址，并继续携带成员访问码。
- 插件版本：`0.4.8`。需要在 Edge 的扩展管理页点击“重新加载”，随后刷新已打开的1688页面。
- 验证：定向回归 21 项通过，JS/Manifest 语法通过；项目自检的唯一失败是既有 `P000005` 结构校验，非本次连接修复引入。Ozon写接口0，库存接口0。

## 2026-07-15 改类目后断点无法继续修复

### 已完成

- 类目修改接口现在会重置运行断点：清空旧 `active_step`、失败信息、重试计数和当前步骤列表，状态回到可继续的 `COLLECTED`。
- 工作台运行按钮只依据真实 `QUEUED/PROCESSING/UPLOADING` 状态禁用，不再因为历史 `active_step` 残留把已重置商品显示成“正在运行”。
- 修复类目属性映射的中文同义词顺序：先把“不锈钢/塑料/玻璃”等中文值映射到俄文字典，再做字典匹配；空候选安全返回 `unknown`，不会触发 `max() iterable argument is empty`。
- P000005 已用当前锁定类目完成本地只读续跑：`17027933 / 97498`，Ozon类目 `api_confirmed`，30项类目属性已生成。

### 当前未完成

- P000005 尚未重新跑完整文字、生图、质检和上传；这是等待用户点击“继续生成”，不是后台仍在运行。
- 项目自检唯一失败为 P000005 结构校验，因为类目修改按规则使旧文字、图片策略和payload失效，必须重新生成。

### 验证与安全记录

- 定向回归74项通过；工作台JavaScript语法检查通过。
- 本轮 Ozon 写接口0、库存接口0；没有调用Ozon写接口，也没有提交库存字段。
- 继续运行时仍遵守：pending/状态不明确禁止重传、失败店铺独立重试、单商品失败不阻塞批次。

### 下一步动作

1. 打开 P000005，点击“继续生成”；从待处理步骤继续，不重新采集、不重复CREATE。
2. 生成阶段若单图或单字段失败，只保留失败原因并提供对应“修改/重试”按钮，其他商品继续。

## 2026-07-16 P0 交接记录（历史快照，已由顶部归档规则取代）

> 以下保留P0最初实施证据；用户随后已明确授权旧商品归档和通道安全停止，当前进程与状态以文档顶部权威节为准。

### P0 已完成

- 交接快照：`runtime/p0-snapshots/20260716-001217/`。
- SQLite 任务状态库：`runtime/task-db.sqlite3`；迁移文件：`migrations/001_task_database.sql`、`migrations/002_local_archive.sql`；最终切换标记：`runtime/task-db-cutover.json`。
- SQLite 已成为唯一可变任务状态源。原有 JSON 发布记录保留为 `.readonly-backup`，用于回滚/审计，不再持续双写。
- 店铺模型拆为 `store_publications` 与 `store_sku_publications`：每个店铺、每个 SKU 独立保存 offer_id、task_id、ozon_product_id、状态、错误和哈希。
- `scripts/remote_status_worker.py`仅作为历史兼容文件保留；工作台不再自动拉起，手工刷新明确返回“远端回查已停用”。
- 工作台首页、运行入口不再同步等待 Ozon；P0 已禁用主工作台自动启动财务同步。财务独立进程和统一启动器拆分留到后续阶段。
- 图片门禁要求：每个已选 SKU 主图齐全，且每个商品 8 张详情图齐全，才允许进入上传；仅做技术硬检查，不做 AI 审美拦截。
- 上传入口每次在写请求前都根据当前 `image-plan.json` 和实际文件重建图片检查，旧 `final-upload-check.json` 的 PASS 不能绕过门禁。新规则不再进行远端回查；临时图片通道独立使用24小时固定TTL。
- SQLite 数据库及 `-wal`/`-shm` sidecar 权限固定为0600，新建、迁移、恢复后都会再次校正。

### 安全边界

- PENDING_REMOTE、SUBMITTED 或状态不明确时，CREATE 重试次数为 0，只允许只读回查。
- 写入前网络/超时/429/5xx 最多自动重试2次；单店失败不影响其他店。
- 本轮未启动真实商品任务，Ozon 写接口 0、UPDATE 0、只读回查0、库存接口 0；归档后不再运行异步 worker。
- P000004/P000005 原有 task_id、商品ID和店铺拆分已迁入 SQLite 并进入归档摘要，未重复提交。

### 当前问题与已知风险

- 该历史快照中的2个 `image_channel_worker` 已按用户授权写stop-file并正常退出；当前真实图片进程0，无需强制终止。
- P000005 的旧 `final-upload-check.json` 已重建为 FAIL：5张SKU主图存在但8张详情图缺失；直接调用上传器会在任何Ozon写请求前阻断，写接口计数为0。
- 已按PID清理13条确认失效的旧 `.pipeline.lock` / `image-status-monitor.pid` 记录；当前没有商品锁或失效PID文件，仍存活的2个图片任务未被终止。
- `tests/test_ozon_field_completion.py` 的部分断言依赖旧 P000004/P000011 运行夹具，当前会出现属性、图片清单和 rich content 期望不匹配；不影响 P0 状态库和上传门禁，但应在后续修复测试夹具。

### 验证记录

- `tests.test_p0_task_database`、`tests.test_multi_store_upload`、`tests.test_batch_confirmation`、`tests.test_collection_inbox`、`tests.test_lan_collaboration`：35项通过。
- 新增 `LiveImageGateTest`：旧 PASS + 缺8张详情图时，上传器在任何网络/写请求前失败，写接口计数0。
- P0组合定向测试：37项通过。全量 `unittest discover -s tests -p 'test*.py'`：390项中321项通过、25项失败、19项错误、25项跳过；失败/错误是既有夹具和历史预期漂移，已与P0门禁、退避、SQLite权限验证分开记录。
- Python 编译及 `workbench.js` 语法检查通过。
- 模拟规模：30商品 / 20类目 / 10店铺 / 每商品3 SKU，聚合状态和店铺隔离通过。

### 下一步动作

1. 由用户明确授权后安全停止存活的图片 worker；在此之前不执行终止。
2. 做 SQLite 回滚演练与旧测试夹具更新。
3. 完成 P0 验收后，再进行真实30商品试跑和95%成功率验收。
