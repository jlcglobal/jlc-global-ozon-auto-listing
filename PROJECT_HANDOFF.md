# PROJECT_HANDOFF

更新时间：2026-07-22

## 项目当前真实方向

AI Factory 是本地 1688 → Ozon 自动上架生产线。当前不做库存、订单、广告、财务运营分析或传统 ERP。

正式商品主链：

```text
工作台采集
→ 用户选择 SKU、最终 Ozon 类目、目标店铺
→ 商品事实合并
→ 属性填值输入
→ ozon-ecommerce-designer
→ 确定性 Ozon 字段编译
→ 图片生成与技术检查
→ 自动建立临时图片公网通道
→ 多店 Ozon 提交
```

## 当前已完成

- 2026-07-20：图片生成失败续跑已改成全局按图位恢复。任何商品的失败/`needs_review` 图位都会重新进入生图队列；已通过图位根据文件、回执和 SHA256 保留，不再重做。
- 2026-07-20：图片失败不再默认把整件商品卡到人工处理；正常失败图位自动继续重试，启动前程序错误单独记录且不消耗真实生图次数。
- 2026-07-20：P000020 当前已从失败主图 `main-5811835345115` 真实恢复到 `image_generation`，子图位 worker 已启动；Ozon/库存调用为 0。
- 建立 `ozon-ecommerce-designer` 作为俄文资料、属性语义填值、图片销售方案的统一策划层。
- 建立商品事实合并、属性填值输入和确定性字段编译方向。
- 工作台商品详情已加入每个已选 SKU 一行的资料编辑表；用户可改颜色、容量、商品/包装重量、商品/包装长宽高、装量和动态 SKU 属性，也可以不改直接运行。
- 工作台 SKU 基础单位统一为 mm、g、ml、件；Ozon 需要 cm、kg、L 或整数时由字段编译器确定性转换。
- SKU 用户修改保存到 `input/workbench-sku-overrides.json`，不会覆盖 `input/source.json`。
- 批次创建时冻结 `output/sku-run-snapshot.json`，保证运行中的商品使用点击运行那一刻的 SKU 资料快照。
- `ozon-attributes-final.json` 已拆分为 `common_attributes` 和 `attributes_by_sku`；上传器按当前 SKU 合并属性和包装数据，不复制第一 SKU。
- 已补充单 SKU、双 SKU、10 SKU 自动测试；10 SKU 达到业务上限时，工作台保存、运行快照、属性编译、图片绑定和 Ozon payload 都按 `sku_id` 明确关联。
- 已完成真实运行中工作台服务验证：商品详情接口返回非空 `sku_row`，SKU 覆盖保存后刷新不丢失，运行快照和 payload 按 SKU 保持差异。
- 商品目录已按 `input/` 与 `output/` 分层，禁止生成输出回流成输入。
- 正式商品只允许读取当前工作台采集输入；测试素材独立放在测试目录。
- 图片规则为动态 `N 张 SKU 主图 + 8 张共享详情图`。
- 用户点击运行任务后不再增加逐字段、逐图或逐阶段人工确认。
- 2026-07-22：用户点击“运行任务”后，新批次强制自动提交。图片技术检查通过即后台建立固定 24 小时临时 HTTPS 图片通道并提交已选店铺，不再等待“确认上传”。
- 2026-07-22：已知 macOS 本机 TLS 探测失败仅记录为本机探测不可用，不再将正在运行的图片公网通道误判为上传前失败；其他真实公网或图片错误仍安全失败。
- 上传前安全检查从独立任务步骤改成上传动作内部实时执行。
- 2026-07-22：全局 Ozon 字段最终修复层已启用：标签只保留合规俄文搜索词（最多 30 个，不强凑数量）；字典字段只提交当前类目允许值；可自动转换的数值单位、类型和范围问题会直接修复，非必填非法字段自动删除。
- 2026-07-22：商品长宽高不会误映射为电源线/线长；仅当本次采集资料明确包含线长事实时才会填写该类属性。每次重建草稿会输出 `output/ozon-field-repair-report.json` 记录字段转换、替换和删除原因。
- 旧分析、打包、质检、俄文文案独立 Skill 已从当前可扫描目录删除。
- 当前执行代码和活跃测试不再引用旧流程标识。

## 最近测试结果

- 图片失败续跑定向测试：24 项通过，1 项跳过。
- 图片/批次/上传相关回归：107 项通过，31 项跳过。
- SKU/属性/payload 定向测试：10 项通过。
- 本轮字段修复定向测试：95 项通过，43 项跳过，失败 0，错误 0。
- 全量回归：525 项通过，86 项跳过，失败 0，错误 0。
- 本地实操验证摘要：`test-data/manual-output/sku-workbench-validation-20260718/summary.json`。
- 十 SKU 边界验证摘要：`test-data/manual-output/sku-10-boundary-20260718/summary.json`。

## 当前未完成

- 需要用新的真实工作台采集商品继续做完整端到端验收，重点验证图片完成后自动建立公网图片地址并自动提交目标店铺的整条链。
- 需要继续观察“继续按钮”是否能从当前失败步骤恢复并显示进度。
- 需要确认插件包与当前采集字段一致。

## 受保护数据

不得删除或改写：

- `products/`
- `batches/`
- `runtime/task-db.sqlite3`
- 历史 payload、任务号、回执、店铺提交记录
- 已经提交给 Ozon 的商品记录
- 原始采集图片和 `input/source.json`

这些数据只作为审计和恢复依据，当前新商品运行不能把旧格式当模板。

## 当前风险点

- 工作区已有大量未提交修改，继续修改必须避免回退用户或其他任务改动。
- 历史商品目录可能包含旧产物，但程序不应把它们作为新商品输入。
- 顶部商品级汇总只可展示，不能再作为最终上传字段来源；最终上传只能读取对应 SKU 行资料。
- 真实 Ozon 提交由用户点击“运行任务”后自动执行；历史已提交商品不会因此重复创建。

## 本轮 Ozon 调用

- CREATE/UPDATE：0
- 只读回查：0
- 库存接口：0
